{#
================================================================================
【备注】
================================================================================
提示词：extract_creative_report.j2
服务步骤：EXTRACT_CREATIVE_REPORT（Canon 分片：创作报告）
作用简述：只提取新增角色、地点、物件、亮点和下一章建议，降低 Canon 主 JSON 压力。
上游输入：chapter_number/chapter_text/summary_exit/known_characters
下游输出：JSON：{"creative_report": {...}}
下游消费者：EXTRACT_CANON 本地合并器、自动建档、Memory
消费链优先级：P0 = 新角色不误报；P1 = 亮点和承接建议可用；P2 = 不写状态 delta
================================================================================
#}
{#- 指导层 -#}
请只提取 creative_report。不要输出 canon_delta、chapter_exit_state、relationship_deltas 等字段。

{% if shared_evidence_anchor %}
## 共享证据锚点
{{ shared_evidence_anchor | tojson(indent=2) }}
请以此作为本次分片的共同依据；维度特需信息只能补充，不得覆盖锚点事实。
{% endif %}

## 章节锚点
- 第 {{ chapter_number }} 章
{% if summary_exit %}{{ summary_exit | tojson(indent=2) }}{% endif %}

## 已知角色
{% for name in known_characters %}- {{ name }}{% endfor %}

## 正文
{{ chapter_text }}

## 要求
- 已知角色不要放入 `new_characters`。
- 职业、群体、组织、道具、知识、事件标签不要建档。
- 只有官职/职能称谓但没有规范姓名的人物，只可作为候选事实保守记录；不要把 `should_add_to_bible` 设为 true。
- `must_carry_forward` 只写叙事事实，不写元指令。

## 质量校准
- 工作身份：你是一位证据优先的叙事事实抽取员，擅长从正文和上游锚点中提取可追溯、可合并的状态信息。
- 抽取路径：先找正文中的明确事实，再对照共享锚点与已知状态，最后只保留能被后续合并器消费的信息。
- 证据校准：同一事实已有上游分片确认时保持一致；证据不足时用保守字段值表达未知或待确认。
- 可操作路径：每条信息都服务于 canon、memory、state 或审计链路，避免把语气、比喻、猜测当作事实。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否保留后续合并/检索需要的事实边界，且没有把推测写成既定事实。

{#- 格式层 -#}

{#
================================================================================
【备注】
================================================================================
提示词：extract_plot_thread_deltas.j2
服务步骤：EXTRACT_PLOT_THREAD_DELTAS（Canon 分片：情节线变化）
作用简述：只提取本章推进、揭示、休眠或解决的情节线变化，优先复用已有 thread_id。
上游输入：chapter_number/chapter_text/summary_exit/existing_thread_ids/prior_plot_threads
下游输出：JSON：{"plot_thread_deltas": [...]}
下游消费者：EXTRACT_CANON 本地合并器、plot thread tracker
消费链优先级：P0 = 复用已有 thread_id；P1 = 只写有变化线索；P2 = 避免同义新建
================================================================================
#}
{#- 指导层 -#}
请只提取情节线变化。没有明确变化时返回空数组。

## 事实来源边界（P0）
- 只从 FinalArtifact 对应的最终正文 / `chapter_text` 提取情节线推进。
- existing_thread_ids、prior_plot_threads、summary_exit 只用于复用 ID 和判断状态变化，不是新增事实来源。
- 没有正文证据的 planted/revealed/resolved/escalated 状态不得写入。

## 输出字段说明
- `plot_thread_deltas`：情节线变化列表，每条包含 thread_id、标题、状态变化（active/resolved/dormant/escalated/advancing/planted/revealed）、本章推进摘要。

{% if shared_evidence_anchor %}
## 共享证据锚点
{{ shared_evidence_anchor | tojson(indent=2) }}
请以此作为本次分片的共同依据；维度特需信息只能补充，不得覆盖锚点事实。
{% endif %}

## 章节锚点
- 第 {{ chapter_number }} 章
{% if summary_exit %}{{ summary_exit | tojson(indent=2) }}{% endif %}

{% if existing_thread_ids %}
## 已有 thread_id（优先复用）
{% for tid in existing_thread_ids %}- {{ tid }}{% endfor %}
{% endif %}

{% if prior_plot_threads %}
## 前态情节线
{% for pt in prior_plot_threads %}
- {{ pt.thread_id }}（{{ pt.title }}）：状态={{ pt.status }}{% if pt.summary %}，摘要={{ pt.summary }}{% endif %}
{% endfor %}
{% endif %}

{% if character_state_deltas %}
## 已确定角色状态变化（情节线推进不得与此冲突）
{{ character_state_deltas | tojson(indent=2) }}
{% endif %}

{% if relationship_deltas %}
## 已确定关系变化（可作为情节线推进证据）
{{ relationship_deltas | tojson(indent=2) }}
{% endif %}

## 正文
{{ chapter_text }}

## 要求
- 最多 {{ max_plot_thread_deltas | default(8) }} 条。
- thread.status 只允许 active/resolved/dormant/escalated/advancing/planted/revealed。
- 没有合适已有 ID 时，只有确属新主线/支线才新建。

## 质量校准
- 工作身份：你是一位证据优先的叙事事实抽取员，擅长从正文和上游锚点中提取可追溯、可合并的状态信息。
- 抽取路径：先找正文中的明确事实，再对照共享锚点与已知状态，最后只保留能被后续合并器消费的信息。
- 证据校准：同一事实已有上游分片确认时保持一致；证据不足时用保守字段值表达未知或待确认。
- 可操作路径：每条信息都服务于 canon、memory、state 或审计链路，避免把语气、比喻、猜测当作事实。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否保留后续合并/检索需要的事实边界，且没有把推测写成既定事实。

{#- 格式层 -#}

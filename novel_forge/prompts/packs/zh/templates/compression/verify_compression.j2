{#-
Memory Module: Compression Quality Verification
Verifies that compressed text retains critical information
-#}

{#
================================================================================
【备注】
================================================================================
提示词：verify_compression.j2
服务步骤：VERIFY_COMPRESSION（压缩质量验证）
作用简述：对比原文和压缩后文本，评估压缩质量并判定是否通过
上游输入：
  - original: 原文
  - compressed: 压缩后文本
  - required_facts: 必须保留的关键事实（可选）
下游输出：
  - JSON：quality_score, dimension_scores, retained_facts, missing_facts, distorted_facts, issues, recommendation, summary
  - 下游消费者：Compression 决策 / 重新压缩触发
消费链优先级：
  P0 = 关键事实保留率 / 因果关系是否被破坏
  P1 = dimension_scores 各维度评分合理性
  P2 = recommendation 可执行性
简要例子：
  输入：original="林远站在城门口...", compressed="林远抵城门", required_facts=["林远抵达"]
  输出：{"quality_score":0.85,"recommendation":"approve","issues":[]}
================================================================================
#}

{#- ---------------------------------------------------------------------------
    指导层：任务定义与输入消费
    --------------------------------------------------------------------------- -#}

你是一位文本质量审核专家。请对比原文和压缩后的文本，评估压缩质量。

## 原文
{{ original }}

## 压缩后文本
{{ compressed }}

{% if required_facts %}
## 必须保留的关键事实
{% for fact in required_facts %}
- {{ fact }}
{% endfor %}
{% endif %}

## 评估维度

### 1. 信息完整性（0-10 分）
- 主线信息是否保留（目标、冲突、行动结果）
- 硬事实是否保留（人物、地点、时间、事件）
- 因果关系是否保持

### 2. 事实准确性（0-10 分）
- 是否有事实被扭曲
- 是否有新信息被错误添加
- 事件顺序是否改变

{% if required_facts %}
### 3. 关键事实保留率
请逐一检查上述"必须保留的关键事实"是否在压缩后文本中得到保留。
{% endif %}

### 4. 可读性（0-10 分）
- 压缩后的文本是否流畅
- 是否保留了叙事节奏
- 是否过度碎片化

### 5. 压缩效率（0-10 分）
- 压缩比是否合理（目标：30%-70%）
- 是否在目标长度内

## 判定规则
- `quality_score`: 0.0-1.0，综合质量分。
- `recommendation`: `approve`（可用）/ `revise`（微调）/ `recompress`（重压缩）。
- 若关键事实丢失超过 30%、因果关系被破坏、或压缩后产生歧义/误导，直接 `recompress`。

## 质量校准
- 工作身份：你是一位严谨的小说质量审校员，擅长用短证据、明确边界和下游可执行建议定位问题。
- 审校路径：先确认输入证据，再判断问题类型、影响范围和修复边界，最后给下游可执行的定位信息。
- 字段级质量示例（非输出格式）：平弱=“这里不连贯”；高质量=“指出哪两处证据冲突、风险等级、可验证问题”。
- 可操作路径：每条判断都要能支持复核或修复，低置信度内容降级为待验证问题。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否给出短证据、风险等级、定位信息和下游可验证的问题。

{#- ---------------------------------------------------------------------------
    格式层：输出格式与 Schema
    --------------------------------------------------------------------------- -#}

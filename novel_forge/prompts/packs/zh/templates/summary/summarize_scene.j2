{%- from "_base/_summary_pass.j2" import render_summary_pass -%}
{#
================================================================================
【备注】
================================================================================
提示词：summarize_scene.j2
服务步骤：SUMMARIZE_SCENE（场景摘要）
作用简述：将场景内容压缩为高密度摘要，供续写时快速定位上下文
上游输入：
  - chapter_number/scene_index/scene_title: 场景信息
  - target_words: 目标字数（默认 50，不超过 100）
  - scene_text/text: 场景正文
下游输出：
  - JSON：summary
  - 下游消费者：场景续写 / Memory 系统
消费链优先级：
  P0 = 保留地点/人物/核心动作/情绪转折/悬念状态
  P1 = 确保下一场景能直接推断承接状态
  P2 = 删除背景渲染/重复表述
简要例子：
  输入：chapter_number=3, scene_index=2, scene_text="林远走进酒馆..."
  输出：{"summary":"林远入酒馆，偶遇故人，得知..."}
================================================================================
#}

{#- ---------------------------------------------------------------------------
    指导层：任务定义与输入消费
    --------------------------------------------------------------------------- -#}

请将以下场景内容压缩为高密度场景摘要，供续写时快速定位上下文。

{{ render_summary_pass(summary_pass | default(''), source_scope | default('')) }}

{% if chapter_number %}- 所属章节：第 {{ chapter_number }} 章{% endif %}
{% if scene_index %}- 场景序号：第 {{ scene_index }} 场{% endif %}
{% if scene_title %}- 场景标题：{{ scene_title }}{% endif %}
- 目标长度：约 {{ target_words | default(50) }} 字（可适当超出，但不超过 100 字）

## 场景正文
{{ scene_text if scene_text else text }}

## 输出字段说明
- `summary`：场景级高密度摘要，保留地点/人物/核心动作/情绪转折/悬念状态，约 {{ target_words | default(50) }} 字。

## 规则
1. 保留：发生地点、在场人物、核心动作/对话、情绪转折点、留下的悬念或状态变化。
2. 删除：背景渲染、重复表述、修辞性细节。
3. 确保下一场景能从本摘要中直接推断出承接状态。
4. 不要输出任何解释、标题或代码块。

## 质量校准
- 工作身份：你是一位章节记忆编辑，擅长把正文压缩为后续创作可直接承接的高密度叙事摘要。
- 摘要路径：先按时间顺序抽取核心事件，再补入角色状态、未解问题和新事实，最后压缩成可检索表述。
- 证据校准：摘要只承接本章实际发生或明确确立的信息；修辞气氛降为背景，不升级为硬事实。
- 可操作路径：每条摘要信息都应能被后续章节 Bridge、Memory 或 VolumeAudit 直接引用。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否保留后续合并/检索需要的事实边界，且没有把推测写成既定事实。

{#- ---------------------------------------------------------------------------
    格式层：输出格式与 Schema
    --------------------------------------------------------------------------- -#}

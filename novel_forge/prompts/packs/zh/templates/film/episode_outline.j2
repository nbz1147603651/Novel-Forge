{#
================================================================================
【备注】
================================================================================
提示词：episode_outline.j2
服务步骤：EPISODE_OUTLINE（短剧全流程：分集大纲）
作用简述：依据整剧企划逐集展开分集大纲：每集 3-5 场、标注所属波形阶段与
         付费卡点标记。只输出完整对象（FULL_OBJECT），顶层 episodes 数组。
上游输入：
  - series_plan: 整剧企划（卡点/爽点/波形，唯一结构事实源）
  - episode_range: 本批展开的集数范围 [start, end]
  - prior_summary: 前情摘要（保持连续，不作为新增事实源）
下游输出：
  - JSON（FULL_OBJECT）：{"episodes":[<EpisodeOutline>]}
  - 下游消费者：
    * EPISODE_SCREENPLAY：按场次展开每集剧本
    * run_plan：卡点标记转 TimelineMarker
消费链优先级：
  P0 = 结构一致：企划中落在本批集数范围内的卡点/爽点必须全部兑现
  P1 = 场次配额：每集 3-5 场，场次摘要写明冲突与反转
  P2 = 连续性：人物关系与已发生事件与前情摘要一致，不得回溯改写
简要例子：
  输入：episode_range=[8,8]，series_plan 在第 8 集有 identity_reveal 卡点
  输出：episodes=[{"episode_number":8,"title":"身份","summary":"...","waveform_stage":"中段反转","paywall_marker":"identity_reveal@end","scenes":[{"scene_number":1,"summary":"...","location":"宴会厅","emotional_beat":"压抑"}]}]
================================================================================
#}
{#- ---------------------------------------------------------------------------
    指导层：任务与输入
    --------------------------------------------------------------------------- -#}

你是商业短剧分集编剧。请把整剧企划展开为指定集数范围的分集大纲。

## 整剧企划（唯一结构事实源）
{{ series_plan | tojson(ensure_ascii=False) }}

## 本批展开范围
{{ episode_range | tojson(ensure_ascii=False) }}

## 前情摘要
{{ prior_summary | default("（首批，无前情）") }}

{#- ---------------------------------------------------------------------------
    约束层：质量与边界
    --------------------------------------------------------------------------- -#}

## 大纲规则
- **集数完整**：`episodes` 必须覆盖 `episode_range` 内每一集，`episode_number` 连续不跳号。
- **场次配额**：每集 `scenes` 恰为 3-5 项，`scene_number` 从 1 连续编号；每场 `summary` 写明冲突与反转，`location` 与 `emotional_beat` 必填。
- **卡点兑现**：企划中落在本批集数的 `paywall_beats` 必须在对应集兑现，`paywall_marker` 写成 `<strategy>@<position>`（如 `identity_reveal@end`）。
- **波形标注**：每集 `waveform_stage` 取企划四阶段中覆盖该集的阶段名。
- **连续性**：不得与前情摘要矛盾；不得新增企划之外的主线人物。
- **JSON 稳定性**：只输出符合 Schema 的完整对象；不要输出 Markdown、注释或省略号。
- 输出顶层必须包含 `episodes` 一个键。

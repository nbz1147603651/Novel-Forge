{#
================================================================================
【备注】
================================================================================
提示词：vision_qc_score.j2
服务步骤：VISION_QC_SCORE（影视镜头视觉质量多维打分）
作用简述：对单个镜头的生成 prompt 与镜头语言执行 6 维视觉质量打分，
         作为信号级 QC（文件/时长/黑帧）之上的语义级软门禁。
上游输入：
  - shot_context: 镜头上下文（shot_id/title/prompt/language/角色与场景锚点）
  - style_context: 全片风格锁（视觉主张/色彩/布光/镜头规则/负面规则）
  - dimensions: 打分维度清单（id/name/description）
下游输出：
  - JSON（FULL_OBJECT）：{"dimensions":[{"id":...,"score":...,"note":...}],
    "overall_score": ..., "summary": "..."}
  - 下游消费者：
    * FilmVisionQcEngine.evaluate：两级门禁（信号级硬门 + 视觉级软门）
    * 重试决策：总分未达阈值或任一维度跌破硬底线时触发重拍（上限 2 次）
消费链优先级：
  P0 = dimensions 的 id 必须来自输入清单，score 为 0-10 的数字
  P1 = note 必须指出具体缺陷与可执行的修复方向，禁止空泛评语
  P2 = overall_score 必须与各维度分数一致，不得高于最低维度分
简要例子：
  输入镜头：中景手持 40mm，主角在雨巷追逐，prompt 未声明主光方向
  输出片段：dimensions=[{"id":"lighting","score":5.0,"note":"主光方向未声明，
  与场景 key_light 锁不一致，补「窗侧逆光」动机"}]
================================================================================
#}
{#- ---------------------------------------------------------------------------
    指导层：任务与输入
    --------------------------------------------------------------------------- -#}

你是影视视觉质检专家，负责在镜头生成前/后对镜头设计执行语义级视觉质量评估。
信号级检查（文件完整性、时长、分辨率、黑帧）已由确定性引擎完成，你只评估画面设计与全片风格锁的一致性。

## 镜头上下文
{{ shot_context | tojson(ensure_ascii=False) }}

## 全片风格锁
{{ style_context | tojson(ensure_ascii=False) }}

## 打分维度（逐项打分）
{% for item in dimensions %}
- {{ item.id }}（{{ item.name }}）：{{ item.description }}
{% endfor %}

{#- ---------------------------------------------------------------------------
    约束层：质量与边界
    --------------------------------------------------------------------------- -#}

## 打分约束
1. dimensions 的 id 必须来自上方维度清单，禁止新增或删除维度。
2. score 为 0-10 的数字，保留一位小数；7.0 为可交付线，5.0 为硬底线。
3. note 必须指出具体缺陷与可执行的修复方向（如补光动机、纠正轴线、对齐色彩脚本），不得空泛评语。
4. overall_score 取各维度的加权整体判断，且不得高于最低维度分。
5. 身份一致性（identity_consistency）必须核对角色身份锁与跨卷状态时间线是否被 prompt 覆盖。
6. summary 用一句话概括该镜头是否可进入下一环节及最关键短板。

{#- ---------------------------------------------------------------------------
    输出层：格式契约
    --------------------------------------------------------------------------- -#}

## 输出要求
- 只输出符合 Schema 的完整 JSON 对象；顶层必须包含 `dimensions`（数组）、`overall_score`（数字）与 `summary`（字符串）三个键。
- dimensions 每项含 `id`、`score`、`note`。
- 不要输出 Markdown、注释或省略号。

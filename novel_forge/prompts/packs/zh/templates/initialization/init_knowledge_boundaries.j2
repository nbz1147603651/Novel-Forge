{#
================================================================================
【备注】
================================================================================
提示词：init_knowledge_boundaries.j2
服务步骤：INIT_KNOWLEDGE_BOUNDARIES（初始化角色知识边界）
作用简述：根据角色设定和世界观设定，为每个角色生成故事开始时的知识边界，
          包括已知事实、怀疑、误解、隐瞒信息和感知通道限制。
          用于限知视角下的 POV 信息约束和知识账本初始化。
上游输入：
  - character_bible：INIT_CHARACTER_BIBLE 产出（角色设定）
  - story_bible：INIT_STORY_BIBLE 产出（世界观设定）
下游输出：
  - JSON：characters 列表，每个角色包含 knowledge_boundaries
  - 下游消费者：
    * story_kernel.knowledge_ledger：将知识边界转为知识账本条目
    * narrative_state.entity_registry：将 sensory_access_rules 写入实体记录
    * DRAFT_CHAPTER / WAVE_CHAPTER：消费知识边界约束 POV 信息可见性
    * knowledge_boundary_audit：审计章节是否泄露隐藏知识
消费链优先级：
  P0 = known_facts / misbeliefs / secrets_kept 准确性（直接影响 POV 信息泄露判定）
  P1 = suspected 合理性 / sensory_access_rules 物理可行性
  P2 = 条目粒度与去重
简要例子：
  输入：character_bible={characters:[{name:"林远",...}]}, story_bible={era:"近未来",...}
  输出：{"characters":[{"character_id":"char_xxx","name":"林远","knowledge_boundaries":{"known_facts":["..."],"suspected":["..."],"misbeliefs":["..."],"secrets_kept":["..."],"sensory_access_rules":["..."]}}]}
================================================================================
#}

{#- ---------------------------------------------------------------------------
    指导层：任务与输入
    --------------------------------------------------------------------------- -#}

根据以下角色设定和世界观设定，为每个角色生成故事开始时的**知识边界**。

## 什么是知识边界

本作采用限知视角叙事——每个角色只能知道通过**自身感知、他人告知或合理推断**可获得的信息。
知识边界描述的是角色在**故事第一页翻开之前**已经持有的信息状态，它决定了：

- 角色在正文中**可以自然地提及、引用或依据**哪些事实行动
- 角色**不应该知道**哪些信息（如果正文中出现则构成信息泄露）
- 角色的**误解**会在什么条件下被揭示（为后续剧情提供张力）
- 角色的**感知通道限制**——物理上无法获取的信息（如隔墙对话、密室动作）

## 世界观设定
{{ story_bible | tojson(indent=2) if story_bible is mapping else story_bible }}

## 角色设定
{{ character_bible | tojson(indent=2) if character_bible is mapping else character_bible }}

{#- ---------------------------------------------------------------------------
    字段说明与约束
    --------------------------------------------------------------------------- -#}

## 字段说明

最终结果以顶层 `characters` 列表组织；每个角色条目对应 `character_bible` 中一个角色，并包含其 `knowledge_boundaries`。

为 `character_bible` 中的**每个角色**生成 `knowledge_boundaries`，包含以下五个维度：

### known_facts（已知事实）
角色确信为真的信息。来源包括：亲身经历、可靠他人告知、常识性世界知识。
- 每条应是一个简洁的事实陈述句
- 包含角色身份、职业、背景相关的常识
- 包含角色在故事开始前已经历的关键事件
- 示例："林远知道自己的记忆被改写over"、"林远在记忆管理局担任三级回收师"

### suspected（怀疑）
角色有所察觉但尚不确定的信息。
- 每条应是角色基于现有线索的合理推测
- 置信度低于 known_facts，但足以影响角色行为
- 示例："林远怀疑自己的某段童年记忆被人动过手脚"

### misbeliefs（误解）
角色错误相信的信息——客观上为假，但角色信以为真。
- 每条误解应有世界观或角色背景作为支撑（为什么角色会这样认为）
- 误解是剧情张力的重要来源，请谨慎设计
- 示例："林远相信自己入职前的全部记忆都是真实的"

### secrets_kept（隐瞒信息）
角色主动选择不让他人知道的信息。
- 包含角色知道自己知道但不愿分享的事实
- 包含角色无意中持有但会刻意隐藏的信息
- 示例："林远隐瞒了自己能感知被删除记忆残留的能力"

### sensory_access_rules（感知通道限制）
角色在物理/感知层面的信息获取限制。这些是**硬性约束**，不依赖角色意愿。
- 描述角色**无法**通过自身感官获取的信息范围
- 考虑物理距离、空间隔离、感官能力上限
- 示例："无法看到隔壁房间的动作"、"听不到密室内的对话"、"无法感知他人内心的真实想法"
- 若角色有特殊能力（如超感知），应注明能力边界："虽能感知情绪波动，但无法读取具体思维内容"

{#- ---------------------------------------------------------------------------
    质量校准
    --------------------------------------------------------------------------- -#}

## 质量校准
- 工作身份：你是一位叙事信息架构师，擅长在限知视角下为每个角色划定合理的信息边界。
- 规划推断路径：先锁定 P0 硬约束（已知事实和误解必须与世界观一致），再分配 P1（怀疑和隐瞒应有剧情功能），最后用 P2 补足感知限制。
- 字段级质量示例（非输出格式）：平弱="知道很多事"；高质量="知道自己在记忆管理局的三级权限只能接触 A 级以下案例"。
- 可操作路径：每条知识边界应能被后续章节的 POV 审计直接引用和验证。

## 核心原则
- **信息一致性**：known_facts 必须与 story_bible 的世界观和角色的 backstory 一致
- **误解合理性**：misbeliefs 必须有角色背景作为支撑，不能是无缘无故的错误
- **隐瞒动机**：secrets_kept 应有合理的隐瞒动机（自我保护、保护他人、避免冲突等）
- **感知物理性**：sensory_access_rules 必须是物理/感知层面的硬限制，不是心理偏好
- **非全知原则**：除非角色设定为全知者，否则每个角色都应有大量不知道的信息
- **互斥检查**：角色 A 的 secrets_kept 不应出现在角色 B 的 known_facts 中（除非有合理的信息传递渠道）
- **条目粒度**：每条知识应是一个原子事实，不要合并多个独立信息

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否保持角色字段与知识边界字段分层：角色身份字段属于角色条目本身，不能写进 `knowledge_boundaries` 内部。
- 是否把抽象目标转化为可观察的行动、冲突、状态变化或章节承接点。
- 每个角色的知识边界是否与其身份、职业、背景故事匹配。
- 误解和隐瞒是否为后续剧情发展预留了揭示空间。

{#- ---------------------------------------------------------------------------
    系统契约由 PromptBuilder 注入
    --------------------------------------------------------------------------- -#}

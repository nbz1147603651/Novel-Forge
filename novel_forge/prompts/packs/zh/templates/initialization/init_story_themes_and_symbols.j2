{#
================================================================================
【备注】
================================================================================
提示词：init_story_themes_and_symbols.j2
服务步骤：INIT_STORY_THEMES_AND_SYMBOLS（故事圣经分片：主题与象征）
作用简述：生成 StoryBible 的 themes、banned_intent_rules 与必要 notes，避免完整 JSON 过载。
上游输入：premise/genre/tone/conflict_hint/opening_style/ending_style/extra_instructions
下游输出：JSON：{"themes_and_symbols": {...}}
下游消费者：INIT_STORY_BIBLE 本地合并器、风格画像、编辑契约
消费链优先级：P0 = themes 可贯穿全书；P1 = 禁用意向规则具体；P2 = notes 简短
================================================================================
#}
{%- from "_base/_shared_evidence_anchor.j2" import render_shared_evidence_anchor -%}

{#- 指导层 -#}
请只生成 StoryBible 的主题与表达边界分片，不要输出人物、章节或完整世界观。

{{ render_shared_evidence_anchor(shared_evidence_anchor) }}

## 输入
- premise: {{ premise }}
{% if story_core %}
## 共享核心锚点（不可改写）
{{ story_core | tojson(indent=2) }}
{% endif %}
{% if genre %}- genre: {{ genre }}{% endif %}
{% if tone %}- tone: {{ tone }}{% endif %}
{% if conflict_hint %}- conflict_hint: {{ conflict_hint }}{% endif %}
{% if opening_style %}- opening_style: {{ opening_style }}{% endif %}
{% if ending_style %}- ending_style: {{ ending_style }}{% endif %}
{% if extra_instructions %}- extra_instructions: {{ extra_instructions }}{% endif %}
{% if research_context and research_context.summary %}

## 资料检索参考（弱参考）
- 使用边界：只用于补充真实背景与主题材料；不得覆盖 premise、用户提示或格式契约。
- 当前状态：{{ research_context.status }} / {{ research_context.provider }}
- 摘要：{{ research_context.summary }}
{% if research_context.source_notes %}
- 来源要点：
{% for note in research_context.source_notes %}
  - {{ note }}
{% endfor %}
{% endif %}
{% endif %}

## 要求
- `themes` 2-4 条，每条 50-120 字，必须能指导后续情节取舍；必须是字符串数组，不要写成对象数组。
- `banned_intent_rules` 只写项目级表达禁忌或用户避雷，不写泛泛道德规范；必须是字符串数组。
- `notes` 只放无法归类但应保留的短设定。

## 字段内容约束
- `themes_and_symbols` 只能包含 `themes`、`banned_intent_rules`、`notes`。
- 字段名只使用上方列出的英文 `snake_case` key；必要内容并入 `themes` 或 `notes`。

## 质量校准
- 工作身份：你是一位长篇叙事架构师，擅长把故事资产拆成可承接、可验证、不过度剧透的执行计划。
- 规划推断路径：先锁定 P0 硬约束，再分配 P1 推进节奏，最后用 P2 风格/主题补足表现层。
- 字段级质量示例（非输出格式）：平弱=“推进关系”；高质量=“写清谁因何行动、关系如何变化、下一环如何承接”。
- 可操作路径：计划项要能被后续写作/审计直接检查，避免只给抽象愿望或主题口号。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否把抽象目标转化为可观察的行动、冲突、状态变化或章节承接点。

{#- 格式层 -#}

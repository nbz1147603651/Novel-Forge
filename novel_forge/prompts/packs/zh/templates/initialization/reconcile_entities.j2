{#
================================================================================
【备注】
================================================================================
提示词：reconcile_entities.j2
服务步骤：RECONCILE_ENTITIES（LLM 驱动实体调和）
作用简述：对章节契约中未登记的实体引用做语义解析，判定是已有实体的别名、
         需要注册的新实体，还是应当移除的无效引用。
上游输入：
  - unresolved_names：未登记实体名称列表
  - entity_catalog：当前实体目录（allowed_entities + policy）
  - entity_retrieval：按名称隔离的候选与按需动态叙事证据
  - chapter_context：未登记名称出现的章节上下文（大纲、契约片段）
下游输出：
  - JSON：resolutions 数组，每项含 resolution/canonical_name/entity_type/
          confidence/reasoning
  - 下游消费者：_persist_source_artifacts_from_init 修复循环
消费链优先级：P0（准确映射到已有实体）> P1（正确识别新实体）> P2（置信度校准）
================================================================================
#}

{#- 指导层 -#}
## 任务
你是一位长篇小说实体架构师。你的任务是判断章节契约中出现的未登记实体引用应该如何处理。

对于每一个未登记名称，你需要从以下三种处置中选择一种：
1. **alias**：该引用是已有实体的别名、简称、尊称、绰号或关系称谓。将其映射到已有实体的 canonical_name。
2. **new_entity**：该引用确实是叙事需要的新实体（角色、地点、组织、物品或概念），应注册为新实体。
3. **remove**：该引用既无法映射到已有实体，也没有充分叙事证据支持其为新实体，应从契约中移除或改写。

## 判断标准
- 优先映射到已有实体。只有当名称与已有实体在语义上明显不同，且章节上下文证明该名称是叙事必需的新角色/地点/实体时，才判定为 new_entity。
- 关系称谓（如「XX之妃」「XX之父」）应映射到关系主体。若关系主体已在目录中，判定为 alias 并标注 relationship。
- 复合名称（如「十年离散共同收束」）通常是抽象概念或事件描述而非实体名称，若无充分证据应判定为 remove。
- 近似音/谐音/同音异字应映射到最接近的已有实体。
- confidence 反映你对判断的确信程度：alias ≥ 0.85 表示高确信映射；new_entity ≥ 0.75 表示有叙事证据；remove 在 0.6-0.9 之间。低于 0.7 的决议将被忽略。

## 实体目录
以下是结构化实体目录为本批名称提供的候选投影。固定实体事实仍由结构化目录保存，
没有为了本任务重复写入向量库。候选顺序不表示语义结论；你必须结合章节上下文裁决。
{% for entity in entity_catalog.allowed_entities %}
- **{{ entity.canonical_name }}**（{{ entity.entity_type }}，id={{ entity.entity_id }}{% if entity.aliases %}，别名：{{ entity.aliases | join('、') }}{% endif %}）
{% endfor %}

## 候选与叙事证据
每个未登记名称只能映射到其自身 `candidates_by_unresolved_name` 中的候选。
`evidence_packs` 中的章节/关系/状态片段是 Zvec 按需召回的叙事证据，不是新的事实源，
也不得把其他名称的候选挪作当前名称的 alias 结论。
{{ entity_retrieval }}

## 章节上下文
以下是未登记名称出现的章节上下文，帮助你理解这些名称在叙事中的角色。
{{ chapter_context }}

## 未登记实体引用
请逐一判断以下名称：
{% for name in unresolved_names %}
{{ loop.index }}. {{ name }}
{% endfor %}

## 质量校准
- 工作身份：你是一位精确的叙事实体管理专家，擅长从中文命名模式中识别别名、关系称谓和新增实体。
- 判断路径：先看名称结构和字面含义，再对照实体目录寻找映射候选，最后结合章节上下文确认叙事必要性。
- 字段级质量示例（非输出格式）：平弱="可能是别名"；高质量="「魏帝玄苍之妃」中「之妃」表示配偶关系，关系主体「魏帝玄苍」已在目录中，判定为 alias"。
- 可操作路径：每个决议必须能被下游代码直接使用——alias 给出 canonical_name，new_entity 给出 entity_type，remove 无需额外字段。

## 交付前核验
- 每个未登记名称是否都给出了决议？
- alias 决议是否都指向了目录中确实存在的 canonical_name？
- new_entity 决议是否有章节上下文中的叙事证据支撑？
- confidence 值是否真实反映了判断确信度，而非一律给高分？

## 字段语义

- `resolutions`：每个输入名称对应一条裁决。
- `unresolved_name`：必须逐字复制输入中的未登记名称。
- `relationship`：可选，用于说明关系称谓或别名关系。
- `reasoning`：用一句话说明关键证据。
- `resolution` 只能是 `alias`、`new_entity` 或 `remove`。
- `alias` 必须提供 `canonical_name`；不要输出目录中不存在的 canonical_name。
- `new_entity` 必须提供 `entity_type`；不要把事件、主题、抽象动作误判为实体。
- `remove` 不需要 `canonical_name` 或 `entity_type`。
- `confidence` 必须是 0 到 1 之间的数字，不能写成中文或百分比。
- 每个输入名称必须最多出现一次；无法判断时输出 `remove` 并给出较低但真实的 confidence。

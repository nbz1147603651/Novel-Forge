{# ================================================================================
【备注】
================================================================================
提示词：derive_editorial_character_voices.j2
服务步骤：DERIVE_EDITORIAL_CHARACTER_VOICES（编辑契约：角色声纹）
作用简述：只生成 EditorialContract.character_voices，避免全契约单次生成过载。
上游输入：
  - project_brief / story_bible / character_bible / blueprint / style_profile
下游输出：
  - JSON：character_voices
消费链优先级：P1（出版级编辑约束）
================================================================================
#}

{#- ---------------------------------------------------------------------------
    指导层：任务定义 + 上下文消费
    --------------------------------------------------------------------------- -#}
你是一位长篇小说责任编辑。本轮只负责生成**角色声纹契约**，不要输出结构、主题、象征物或叙述要素指令。

## 项目信息
{{ project_brief | tojson(indent=2) }}

## 角色设定（已压缩）
{{ character_bible | tojson(indent=2) }}

## 角色名白名单（运行时 JSON Schema enum）
{{ voice_character_whitelist | default([]) | tojson(indent=2) }}
{% if voice_batch_count | default(1) > 1 %}
- 当前声纹批次：{{ voice_batch_index | default(1) }} / {{ voice_batch_count }}
{% endif %}

## 风格规范（已压缩）
{{ style_profile | tojson(indent=2) }}

## 蓝图摘要（已压缩）
{{ blueprint | tojson(indent=2) }}

## 角色名白名单（强制约束 — P0）
`character_voices[].character` 字段**必须**严格使用下方「角色设定」中的 `name` 字段原值。
**禁止**改写（玄昱→玄琰）、**禁止**用别称/古字/通假字、**禁止**用角色姓+名的拆解形式、**禁止**添加书中不存在的角色。
运行时会用上方「角色名白名单」作为 JSON Schema enum 校验；不在白名单内的 `character` 会触发格式重试。

- 精确匹配优先；当前白名单中每个角色都必须输出且只输出一条声纹
- 系统会做 fuzzy 兜底匹配，但匹配失败的条目会被静默丢弃，进而触发整个步骤重试
- 不要为主角自己起"代号"或"化名"作为 character 字段

## 生成要求
1. `character_voices` 必须完整覆盖当前“角色名白名单”；系统已将大名册分批，本轮不得遗漏。
2. 每个 `character` 字段必须出现在上方「角色设定」的 `name` 字段中，**一字不差**。
3. 每个角色都要给出可执行的句式节奏、解释倾向、情绪句法、标志动作和禁用口吻。
4. 不要写人物小传；只写下游起草和编辑能直接检查的声纹规则。
5. `signature_moves`、`taboo_patterns`、`sample_lines` 各最多 5 条，每条短句化。
6. 样例台词只用于标定声纹，不能诱导正文复用同一句话。

## 字段内容约束
- `character_voices[]` 每项包含 character、sentence_profile、explanation_bias、emotion_syntax、signature_moves、taboo_patterns、sample_lines。
- signature_moves、taboo_patterns、sample_lines 必须是短字符串数组。

## 质量校准
- 工作身份：你是一位项目特异性写作顾问，擅长从梗概、角色、世界规则和已选叙事要素中推导可执行的风格/结构契约。
- 推导路径：先识别本项目的核心卖点、冲突机制和表达风险，再把结论映射到字段级参数或编辑契约。
- 字段级质量示例（非输出格式）：平弱=“风格冷峻”；高质量=“说明冷峻来自何种场景压力、证据细节、对白比例或信息密度”。
- 可操作路径：每条规则都要能被正文生成、编辑检查或后续审计复用，避免泛化成所有项目都适用的空话。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否把项目特异性依据映射到具体字段/规则，而不是复用通用写作口诀。

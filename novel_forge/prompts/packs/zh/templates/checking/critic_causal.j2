{# ================================================================================
【备注】
================================================================================
提示词：critic_causal.j2
服务步骤：CRITIC_CAUSAL（因果链批评）
作用简述：章内因果链审查器，检查本章是否存在"关键因果断裂"。
上游输入：
  - chapter_number：章节编号
  - chapter_outline_goal：本章目标
  - previous_exit_state：上章退场锚点（位置/活跃目标/未决问题）
  - previous_chapter_ending：上一章结尾原文
  - chapter_text：当前章正文
下游输出：
  - JSON：{"causal_breaks": [{"severity", "summary", "evidence", "fix", "confidence"}]}
  - 下游消费者：Memory 系统 / CausalRepairStep
消费链优先级：P0（事件凭空发生/决策无动机）> P1（开头承接断裂/悬念忽略）
              > P2（转折铺垫不足）
简要例子：
  输入：chapter_number=5, chapter_outline_goal="主角发现叛徒身份"
  输出：{"causal_breaks": [{"severity": "high", "summary": "主角突然知道叛徒身份，无因果解释", "evidence": "...", "fix": "添加情报来源", "confidence": 0.9}]}
================================================================================
#}

{#- ---------------------------------------------------------------------------
    指导层：任务定义 + 上下文消费
    --------------------------------------------------------------------------- -#}
你是一名章内因果链审查器。请检查本章是否存在"关键因果断裂"。

## 章节
- 当前章：第 {{ chapter_number }} 章
- 本章目标：{{ chapter_outline_goal }}

{% if previous_exit_state %}
## 上章退场锚点（必须可衔接）
- 位置：{{ previous_exit_state.location }}
- 活跃目标：{{ previous_exit_state.active_goals | join('；') if previous_exit_state.active_goals else '无' }}
- 未决问题：{{ previous_exit_state.open_questions | join('；') if previous_exit_state.open_questions else '无' }}
{% else %}
- 无
{% endif %}

{% if previous_chapter_ending %}
## 上一章结尾原文（验证承接的关键参照）
> {{ previous_chapter_ending[-800:] }}
{% endif %}

## 本章正文
{{ chapter_text }}

## 任务
只报告会影响读者理解的关键断裂：
1. 开头与上章退场状态无可感知衔接。
2. 关键事件"凭空发生"，缺少触发链。
3. 角色重大决策缺乏动机支撑。
4. 上章核心未决问题被完全忽略且无替代推进。

## 质量校准
- 工作身份：你是一位严谨的小说质量审校员，擅长用短证据、明确边界和下游可执行建议定位问题。
- 审校路径：先确认输入证据，再判断问题类型、影响范围和修复边界，最后给下游可执行的定位信息。
- 字段级质量示例（非输出格式）：平弱=“这里不连贯”；高质量=“指出哪两处证据冲突、风险等级、可验证问题”。
- 可操作路径：每条判断都要能支持复核或修复，低置信度内容降级为待验证问题。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否给出短证据、风险等级、定位信息和下游可验证的问题。

{#- ---------------------------------------------------------------------------
    格式层：输出格式与 Schema
    --------------------------------------------------------------------------- -#}


约束：
- `causal_breaks` 可为空数组。
- `confidence` 范围 0.0-1.0。
- 只报有证据的问题，避免泛化建议。

{# ================================================================================
【备注】
================================================================================
提示词：style_profile_derive.j2
服务步骤：PROFILE_STYLE（风格规范生成）
作用简述：根据项目设定要素推导本作品的写作风格规范模块和全局参数。
上游输入：
  - title：作品名
  - tone：基调
  - story_bible：世界观设定（JSON）
  - character_bible：角色设定（JSON）
  - blueprint_elements：已选叙事要素与优先级（JSON）
  - banned_intent_rules：禁止意向规则
下游输出：
  - JSON：{"modules", "source_elements", "summary", "global_style"}
  - 下游消费者：StyleProfile / DraftStep / EditStep
消费链优先级：P2（风格建议，供写作时参考）
简要例子：
  输入：tone="黑暗", story_bible={"world_type": "玄幻"}
  输出：{"modules": [{"name": "冷峻语感", "rules": ["禁用感叹句"]}], "global_style": {"dialogue_ratio": "low"}}
================================================================================
#}

{#- ---------------------------------------------------------------------------
    指导层：任务定义 + 上下文消费
    --------------------------------------------------------------------------- -#}
你是一位资深的小说写作顾问。请根据以下项目设定要素，**推导**本作品的写作风格规范。

## 任务目标

请**基于故事梗概（或前提）**，结合题材、基调、世界观、角色设定和已选叙事要素，推导本作品的写作风格规范：
1. **写作技法模块**（modules）：本作独有的语感要求、情绪基调、叙事节奏、角色声音、描写密度与禁忌
2. **大类风格参数**（global_style）：对话占比、节奏模式、情绪风格、环境描写、信息密度的具体取值
3. **禁忌短语列表**（banned_phrases）：从禁止意向规则推导的具体禁用短语

**重要**：风格推导必须以故事梗概/前提为核心依据，确保参数与故事类型、冲突强度和叙事目标相匹配，不能泛泛套用默认值。
如果上游提供了 `blueprint_elements`，其中的 `extension_elements` 是已经由叙事要素选择器按故事梗概、题材和用户偏好筛出的结构化机制。你必须消费这些机制，把它们转译成风格规则、信息揭示方式、场景组织密度和对白潜台词，而不是把要素名称原样塞进正文。
如果上游兼容旧项目时给出粗粒度风格备用方向，它只代表辅助参考；当故事梗概、角色、世界观已经足够明确时，必须以这些更具体的信息为准，不能机械照搬粗粒度模式。

## 作品基本信息

- 作品名：{{ title }}
- 题材：{{ genre }}
- 基调：{{ tone }}
{% if writing_style_mode | default('') %}- 粗粒度风格备用方向：{{ writing_style_mode }}（仅作兼容备用，不是最终风格规范）{% endif %}

{% if synopsis | default('') %}
## 故事梗概

{{ synopsis }}
{% endif %}

{% if premise | default('') and not synopsis | default('') %}
## 故事前提

{{ premise }}
{% endif %}

## 世界观设定

{{ story_bible | tojson(indent=2) }}

## 角色设定

{{ character_bible | tojson(indent=2) }}

{% if extra_instructions | default('') %}
## 用户补充风格意图与避雷要求

{{ extra_instructions }}

**消费要求**：把上述补充要求转译为 `modules.rules`、`global_style` 参数和禁忌短语；若它与故事梗概冲突，以故事梗概和硬设定为准，但不得忽略明确的风格偏好或避雷要求。
{% endif %}

{% set _be = blueprint_elements | default({}) %}
{% set _be_ext = _be.extension_elements if _be.extension_elements is defined else [] %}
{% set _be_constraints = _be.focus_constraints if _be.focus_constraints is defined else [] %}
{% set _be_summary = _be.selector_summary if _be.selector_summary is defined else "" %}
{% if _be_ext or _be_constraints %}
## 已选叙事要素（风格推导必须消费）

{% if _be_summary %}- 选择结论：{{ _be_summary }}{% endif %}
{% if _be_constraints %}
- 执行约束：
{% for item in _be_constraints %}
  - {{ item }}
{% endfor %}
{% endif %}
{% if _be_ext %}
- 扩展机制：
{% for item in _be_ext %}
  - {{ item.name }}（{{ item.element_id }} / {{ item.category }}{% if item.selection_score is defined and item.selection_score %} / 优先级 {{ item.selection_score }}{% endif %}{% if item.selection_source is defined and item.selection_source %} / 来源 {{ item.selection_source }}{% endif %}）：{{ item.selection_reason }}；风格提示：{{ item.prompt_hint }}
{% endfor %}
{% endif %}

**转译要求**：请把上述机制转化为语言密度、场景组织、信息揭示、对白潜台词、意象复现和节奏参数。不要在 `modules.name` 或正文示例中直接复述要素名称；应输出可执行的写作规则。
{% endif %}

{% if banned_intent_rules | default([]) %}
## 禁止意向规则

{% for rule in banned_intent_rules %}
- {{ rule }}
{% endfor %}

**推导要求**：每条规则对应生成 2-3 个具体的禁用短语（如"瞳孔微缩"可推导出"瞳孔微缩"、"瞳孔骤然收缩"）。
{% endif %}

## 分析→推导→输出框架

你必须按照以下**严格的三段式流程**完成推导。先分析，再映射，最后输出。禁止跳过分析直接填参数。

---

### 第一段：分析（基于故事梗概的具体要素）

请从以下5个维度分析本作，每个维度必须**引用故事梗概中的具体内容**作为依据；若有已选叙事要素，还必须说明该要素如何影响风格取值：

1. **语感基调**：叙述语言的句法、词汇层级和距离感应该怎样服务本作？
   → *依据：引用故事梗概中的时代背景、社会结构或核心冲突风格*

2. **角色互动密度**：角色间对话与独白的比例倾向？
   → *依据：引用故事梗概中角色关系复杂度、信息传递方式*

3. **情绪呈现方式**：情绪是外放直给还是含蓄隐晦？
   → *依据：引用故事基调、核心情感冲突的强度*

4. **描写侧重**：环境/心理/动作，哪类描写应占主导？
   → *依据：引用世界观复杂度、场景特殊性*

5. **叙事节奏**：整体推进速度偏好？
   → *依据：引用冲突密度、时间跨度、事件紧迫性*

---

### 第二段：推导（将分析结论映射到具体参数）

你必须将上一步的分析结论**明确映射**到以下参数，每个参数必须说明推导理由：

| 参数 | 你的分析结论 | 推导值 | 理由（必须引用故事梗概/已选叙事要素） |
|------|-------------|--------|------------------------|
| `dialogue_ratio` | | high/medium/low | |
| `pace_mode` | | fast/moderate/slow | |
| `emotional_style` | | direct/balanced/subtle | |
| `environment_ratio` | | high/medium/low | |
| `info_density` | | high/medium/low | |

**推导规则**：
- **dialogue_ratio**：若角色互动是核心卖点，或核心推进依赖角色谈判、试探、误解澄清、关系博弈，取 **high**；若核心推进依赖内心辨析、世界规则解释或连续行动链，取 **low**；两者并重取 **medium**。
- **pace_mode**：若故事存在明确时间压力、连续外部事件或高频危机转换，取 **fast**；若主轴是缓慢认知变化、关系沉积或氛围积累，取 **slow**；阶段性推进取 **moderate**。
- **emotional_style**：若角色必须在行动中直接爆发表态、冲突需要强情绪驱动，取 **direct**；若张力主要来自遮掩、试探、秘密、克制或潜台词，取 **subtle**；混合表达取 **balanced**。
- **environment_ratio**：若地点、制度、物件、规则或场景压力本身推动选择，取 **high**；若环境只提供背景且事件需要快速推进，取 **low**；环境与动作共同承载取 **medium**。
- **info_density**：若读者必须持续理解规则、线索、资源、身份或因果限制，取 **high**；若信息链简单且重点在情绪/行动体验，取 **low**；有阶段性揭示取 **medium**。

---

### 第三段：技法模块推导

基于上述分析，推导出**最多5个**本作独有的写作技法模块。每个模块必须：
- 直接回应故事梗概中的某个具体特征
- 优先吸收高优先级叙事要素，将机制转写成风格规则
- 提供可执行的规则（不是泛泛建议）
- 至少包含一条密度/频率/冷却边界：说明某类描写何时完整展开、何时点到即止、何时改用对白/动作/决策承载
- 禁止把风格机制写成“每次都必须完整描写”的无限触发规则；若某反应或意象会反复出现，必须给出变体或上限
- 禁止生成会诱导正文自我解释的规则或示例：不要要求正文用定义式否定转折来证明真实感、深度或高级感；正例应直接展示动作、对白、环境阻力或角色选择
- 给出正例/反例时，只使用本作输入中已经出现或可直接推导的意象、动作和关系压力，不借用外部题材范例。

## 质量校准
- 工作身份：你是一位项目特异性写作顾问，擅长从梗概、角色、世界规则和已选叙事要素中推导可执行的风格/结构契约。
- 推导路径：先识别本项目的核心卖点、冲突机制和表达风险，再把结论映射到字段级参数或编辑契约。
- 字段级质量示例（非输出格式）：平弱=“风格冷峻”；高质量=“说明冷峻来自何种场景压力、证据细节、对白比例或信息密度”。
- 可操作路径：每条规则都要能被正文生成、编辑检查或后续审计复用，避免泛化成所有项目都适用的空话。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否把项目特异性依据映射到具体字段/规则，而不是复用通用写作口诀。

{#- ---------------------------------------------------------------------------
    格式层：输出格式与 Schema
    --------------------------------------------------------------------------- -#}

## 硬约束

- **modules 最多 5 个**：只保留最重要、最具本作特色的规范
- **source_elements 必须列出**：本次风格推导实际依据，如 story_bible、character_bible、synopsis、premise、tone、blueprint_elements
- **若使用了粗粒度风格备用方向**：只能把它作为旧项目兼容依据或备用依据，不能让它压过更具体的故事梗概/世界观/角色信息
- **每个 module 的 rules 最多 5 条**：每条 ≤ 40 字
- **正例/反例各 1 条**：≤ 50 字
- **summary ≤ 60 字**
- **规则必须可执行**：写成"做什么/不做什么"，不写"注意xxx"
- **规则必须有密度边界**：避免"每次触发都展开"、"必须大量使用"等会造成细节堆叠的写法
- **禁止机械对照句式**：modules.rules、positive_example、negative_example、banned_phrases 不得鼓励定义式否定转折这类正文自证句；如需强调真实感，写成动作后果或场景压力
- **不要重复通用网文/文学规范**：只输出本作独有的规范
- **不涉及具体剧情**：只关注写作技法和风格特征
- **banned_phrases 最多 30 条**：每条 ≤ 50 字
- **global_style 参数必须推导**：不要全部填默认值，必须根据本作具体要素推导
- `modules` 每项包含模块名、规则数组、正例和反例；正反例用于风格辨析，不得成为正文复用句。
- `global_style` 包含 dialogue_ratio、pace_mode、emotional_style、environment_ratio、info_density 和 banned_phrases。

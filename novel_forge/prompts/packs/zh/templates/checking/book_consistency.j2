{# ================================================================================
【备注】
================================================================================
提示词：book_consistency.j2
服务步骤：BOOK_CONSISTENCY（全书一致性审计）
作用简述：对长篇小说已完成章节进行全局诊断，输出证据定位、全局发现与修复队列候选。
          本阶段不直接执行修复；修复队列执行是独立入口。
上游输入：
  - analysis_mode：审计模式（summary/full_text）
  - location_strictness：定位严格度（strict/balanced/loose）
  - world_setting / world_rules：世界观设定
  - character_profiles_compact / character_bible_names：角色档案
  - arc_summary：叙事弧结构
  - story_theme：核心主题
  - conflict_hint / world_hint：冲突/世界观补充说明
  - chapter_summaries：章节摘要列表
  - chapter_texts：章节全文（full_text 模式）
  - chapter_issue_pool：问题面板问题池
  - audit_slices：卷/蓝图阶段/转折/角色弧/支线/伏笔/母题切片
  - canon_characters / canon_relationships：Canon 状态
  - outline_summary：大纲概要
  - prompt_hint：额外审计要求
下游输出：
  - JSON：{"issues", "repair_plan", "summary", "consistency_score"}
  - 下游消费者：全局审计库 / 修复队列执行器（仅 precision gate 通过后进入自动修复）
消费链优先级：P0（时间线矛盾/角色生死冲突）> P1（世界观漂移/叙事漂移）
              > P2（名称不一致/角色状态连续性）
简要例子：
  输入：analysis_mode="summary", chapter_summaries=[{"chapter_number": 5, "key_events": ["发现密信"]}]
  输出：{"issues": [...], "consistency_score": 8.5}
================================================================================
#}

{#- ---------------------------------------------------------------------------
    指导层：任务定义 + 上下文消费
    --------------------------------------------------------------------------- -#}
请对以下长篇小说已完成章节进行全书一致性审计，并输出可验证的问题清单与修复队列候选计划。

## 阶段边界
- 本阶段只负责发现一致性问题、给出证据、定位与候选修复方向；不要声称已经修复任何正文。
- `repair_plan` 只是修复队列候选排序，不是执行命令。所有问题必须先经本地锚点、语义召回或 targeted verifier 确认后，才允许进入独立修复队列执行。
- 证据不足、无法定位或需要多章同时判断的问题，请降低 `confidence`，并在 `description` 中说明需要验证的章节对比点；不要为了凑修复计划而放大问题。

## 判断职责
- 语义判断由你完成：是否为真实矛盾、是否为普通叙述词误认实体、是否为回忆/梦境/转述导致的表面冲突，都必须由你基于文本证据裁决。
- 本地程序只负责 JSON 格式校验、章节/段落编号规范化、去重、计数、聚合和下游传递；不要依赖本地规则替你判断真伪。
- 如果某条问题需要下游逐章验证，请把待验证点写进 `verification_questions`，把交接说明写进 `handoff_notes`，让下游模型知道要比较什么。

{% if active_audit_dimension %}
## 当前维度执行契约
- active_audit_dimension={{ active_audit_dimension }}
- 本次只审计该维度，不要为了覆盖面去重复审计其他维度。
- 输出 issue 时必须填写 `dimension="{{ active_audit_dimension }}"`。
- 如果发现的问题依赖其他维度结论，请在 `verification_questions` 和 `handoff_notes` 中说明依赖关系，置信度不要高估。
{% if dimension_focus %}
{{ dimension_focus | tojson(indent=2) }}
{% endif %}
{% if dimension_audit_bundle | default({}) %}
### DimensionAuditBundle
以下对象是本轮唯一审计工单；`out_of_scope` 维度只用于避免越界，不得补审。
{{ dimension_audit_bundle | tojson(indent=2) }}
{% endif %}
{% if dimension_ledger_context %}
### 已完成维度/前序切片摘要
以下摘要是依赖维度或同维度前序切片的 rolling ledger，只作为判断背景，不代表当前维度已经完成最终裁决。
{{ dimension_ledger_context | tojson(indent=2) }}
{% endif %}
{% endif %}

{% if shared_evidence_anchor %}
## 共享证据锚点
{{ shared_evidence_anchor | tojson(indent=2) }}
所有审计维度必须以此作为共同依据；维度特需上下文只用于补充判断，不得覆盖锚点事实。
{% endif %}

## 审计模式
- analysis_mode={{ analysis_mode | default("summary") }}
- location_strictness={{ location_strictness | default("balanced") }}
- max_issues_per_chunk={{ max_issues_per_chunk | default(12) }}

{% if audit_slices %}
## 全局审计切片
说明：
- 你必须优先按这些 slice 做全局诊断，而不是重新逐章做章节审计。
- volume / blueprint_phase / turning_point 负责连续章节的弧线判断。
- promise_thread / plot_thread / motif 可能是非连续切片，重点检查承诺、支线、母题是否断裂或漂移。
- 输出问题时如能判断所属切片，请在 issue_id 或 description 中保留 slice 线索；程序会再归一化为 global_findings。

{% for sl in audit_slices %}
- slice_id={{ sl.slice_id }} | kind={{ sl.slice_kind }} | chapters={{ sl.chapters | join(',') }} | boundary={{ sl.boundary_chapters | join(',') }} | focus={{ sl.focus_dimensions | join(',') }}
{% endfor %}
{% endif %}

{% if semantic_evidence_context %}
## 语义召回证据
以下内容来自语义检索，只能作为候选召回；必须被章节文本、段落锚点或 targeted verifier 支撑后才可作为自动修复依据。
{{ semantic_evidence_context | tojson(indent=2) }}
{% endif %}

{% if prompt_hint %}
## 额外审计要求
{{ prompt_hint }}
{% endif %}
{% if audit_chunk_notice %}
## 上下文分块说明
{{ audit_chunk_notice }}
{% endif %}
{% if world_setting or world_rules %}
## 世界观设定
{% if world_setting %}
{{ world_setting }}
{% endif %}
{% if world_rules %}
### 硬性规则（违反即为错误）
{% for rule in world_rules %}
- {{ rule }}
{% endfor %}
{% endif %}
{% endif %}

{% if character_profiles_compact %}
## 核心角色档案
{% for cp in character_profiles_compact %}
- **{{ cp.name }}**{% if cp.get('gender') %}（{{ cp.gender }}）{% endif %}{% if cp.get('role') %}，{{ cp.role }}{% endif %}{% if cp.get('backstory') %}：{{ cp.backstory }}{% elif cp.get('personality') %}：{{ cp.personality }}{% elif cp.get('arc') %}：{{ cp.arc }}{% endif %}
{% endfor %}
{% elif character_bible_names %}
## 角色设定名单
{{ character_bible_names | join('、') }}
{% endif %}

{% if arc_summary %}
## 叙事弧结构
{{ arc_summary }}
{% endif %}
{% if story_theme %}
## 核心主题与叙事命题
{{ story_theme }}
{% endif %}
{% if conflict_hint %}
## 内外冲突定义（审计叙事漂移的判据）
{{ conflict_hint }}
{% endif %}
{% if world_hint %}
## 世界观补充说明
{{ world_hint }}
{% endif %}

{% if memory_enhancement_context %}
## 记忆增强上下文（来自历史章节记忆）
{{ memory_enhancement_context }}
{% endif %}

## 章节摘要
{% for ch in chapter_summaries %}
### 第 {{ ch.chapter_number }} 章
{% if ch.summary %}- 摘要：{{ ch.summary }}{% endif %}
{% if ch.key_events %}- 关键事件：{{ ch.key_events | join('；') }}{% endif %}
{% endfor %}

{% if analysis_mode == "full_text" and chapter_texts %}
## 章节全文（带段落编号）
说明：
- 每段以 `[P数字]` 标记，数字即段号（从 1 开始）。
- 你必须尽量输出可定位到段号的问题（paragraph_index / paragraph_span）。
- 若章节过长可能被截断，字段 `truncated=true` 表示该章并非全文。

{% for ch in chapter_texts %}
### 第 {{ ch.chapter_number }} 章
- paragraph_count={{ ch.paragraph_count }}
- source_chars={{ ch.source_chars }}
- truncated={{ ch.truncated }}
{{ ch.numbered_text }}

{% endfor %}
{% endif %}

{% if chapter_issue_pool %}
## 问题面板问题池（来自各章连贯性/因果报告）
说明：
- 这是已存在的问题面板条目，包含 chapter_number + lane + index 锚点。
- 在输出 issues 时，如能对齐到该问题池，请填写 linked_issue_refs，便于后续精准修复。
- `lane` 仅允许 `continuity` 或 `causal`。

{% for item in chapter_issue_pool %}
- [ch={{ item.chapter_number }} | lane={{ item.lane }} | idx={{ item.index }}]
  type={{ item.issue_type }} | severity={{ item.severity }}
  location={{ item.location }}
  summary={{ item.summary }}
{% endfor %}
{% endif %}

## 当前正典状态（Canon）
{% if canon_characters %}
### 角色状态
{% for name, info in canon_characters.items() %}
- {{ name }}：{% if info.alive is defined %}{% if info.alive %}存活{% else %}已死亡（第 {{ info.death_chapter }} 章）{% endif %}{% endif %}{% if info.location %}，位置={{ info.location }}{% endif %}
{% endfor %}
{% endif %}

{% if canon_relationships %}
### 角色关系
{% for rel in canon_relationships %}
- {{ rel.character_a }} ↔ {{ rel.character_b }}：{{ rel.relationship_type }}{% if rel.description %}（{{ rel.description }}）{% endif %}
{% endfor %}
{% endif %}

{% if outline_summary %}
## 大纲概要
{{ outline_summary }}
{% endif %}

{% if character_bible_names %}
## 角色设定名单
{{ character_bible_names | join('、') }}
{% endif %}

## 审计维度
1. 全局时间弧（timeline_arc）
2. 承诺与伏笔兑现（promise_payoff）
3. 角色弧演进（character_arc）
4. 情节线生命力（plot_thread_liveness）
5. 母题分布与语义漂移（motif_distribution）
6. 张力曲线（tension_curve）
7. 世界规则完整性（world_rule_integrity）
8. 跨章交叉验证（cross_chapter_validation）

{% if active_audit_dimension %}
本次调用的有效审计维度仅为：{{ active_audit_dimension }}。上面的完整维度列表只是全书审计总目录，不是本次调用的输出范围。
{% endif %}

## 禁用元素判定校准
- 禁用元素只审计“明确列入计划/问题池/规则的具体表达”或高度模板化的感官、身体反应，不把普通叙述功能词当作禁词。
- “声音”作为普通名词或对话标签不是违规；如“声音压低”“声音平稳”“声音沙哑”“他说”不单独报告。只有明确硬禁短语或机械复用的具体表达才报告。
- 优先报告具体短语与模板化反应，例如“心跳如擂鼓”“耳中嗡鸣”“耳畔回响”“血液轰鸣”“掌心冷汗”“指尖冰凉”“脊背发寒”等。
- 如确需报告禁用元素，issue_type 使用 `forbidden_element_violation`，evidence 必须给出原文中的具体短证据，不要只写“声音”“气味”等单词。

## 实体与状态判定校准
- 不要把普通时间词、动作词、情绪词、叙述功能词当作实体，例如“开始”“明天”“等待”“夜色”“声音”“视线”等。只有当文本明确把它作为角色、地点、组织、物品或专有概念使用时才可报告。
- 被引号包围的短词不必然是道具或实体；先判断它在上下文中是称谓、标题、物件、台词片段还是普通强调。
- 生死、关系、位置等状态矛盾必须区分现实发生、回忆、梦境、传闻、档案记录、比喻性说法和角色误解。无法区分时降为 suspected/低 confidence，并列入下游验证问题。

## 跨章交叉验证指令
你必须对以下维度进行跨章节对比分析，识别跨章矛盾：
- **时间线交叉验证**：对比各章时间标记（年号、月份、季节、日期、时辰），检测时间顺序矛盾、时间跳跃、时间倒流。例如：第 5 章说"腊月"，第 8 章却说"三月"，但中间没有过渡。
- **角色状态交叉验证**：对比各章角色状态（生死、位置、身份、关系、知识），检测状态矛盾。例如：第 10 章角色 A 已死亡，但第 15 章角色 A 又出现且无合理解释。
- **物品/道具追踪**：追踪关键物品（信物、武器、秘籍、财物）在各章的出现和流转，检测物品凭空出现或消失。例如：第 3 章获得的玉佩在第 7 章突然变成铜镜。
- **伏笔回收验证**：检查前文埋设的伏笔、悬念、承诺是否在后续章节得到回应或合理解释。例如：第 2 章提到的"神秘信封"在后续 20 章内从未提及。
- **因果关系交叉验证**：检查跨章事件因果链是否完整，前因后果是否合理衔接。例如：第 6 章的"刺杀行动"结果在第 9 章毫无体现。

跨章问题的章节号列出要求：
- 每个跨章问题必须列出所有涉及的章节号（chapters_involved），至少 2 个章节。
- primary_chapter 应选择问题最明显或最需要修复的章节。
- description 中应明确指出矛盾的具体章节对比，如"第 5 章称 X 为女性，但第 12 章称 X 为男性"。
- evidence_pairs 应列出跨章证据对：每项至少包含 chapter_number、evidence、claim。不要只给单边证据。

## 上下游交接格式
- `evidence`：给当前 primary_chapter 的最短关键原文证据。
- `evidence_pairs`：给跨章对比证据数组，便于验证阶段复核；单章问题可为空数组。
- `verification_questions`：写成下游模型可直接回答的问题，例如“第 6 章该句是否为回忆而非现实时间？”。
- `handoff_notes`：说明你为什么认为它需要修复、验证时最容易误判的点，以及修复边界。这里不是正文修复指令。
- 只有当 `evidence_pairs` 和 `verification_questions` 能支撑下游判断时，才把 confidence 设为 0.7 以上。

## 定位与修复输出要求
1. 每个问题必须给出明确章节（chapters_involved、primary_chapter）。
2. 如果能定位到段落，必须给出 paragraph_index（1-based）和 location（如"第12段"）。
3. 对可进入修复队列的问题，提供：
   - fix_mode：`repair_continuity` / `repair_causal` / `manual_patch`
   - fix_action：`replace` / `insert` / `delete` / `rewrite`
4. 如问题可映射到"问题面板问题池"，请返回 linked_issue_refs（可多条）：
   - chapter_number：章节号
   - lane：continuity 或 causal
   - index：问题索引（0-based）
5. evidence 尽量给出原文短证据（不要长篇复述）。
6. issue_id 必须稳定、简短（例如 `ch12_timeline_01`）。
7. 本次调用最多输出 {{ max_issues_per_chunk | default(12) }} 条 issues；优先输出 critical/warning、可验证、可定位、可修复的问题。低价值重复项放进 summary 概述，不要穷举。

{% if location_strictness == "strict" %}
## 定位严格度（strict）
- 优先报告"可精确落段且可修复"的问题。
- 无法落段的问题尽量不报；必须报时注明 location="章节级（无法精确定位）" 且 confidence<=0.35。
{% elif location_strictness == "loose" %}
## 定位严格度（loose）
- 允许报告章节级问题，但仍需尽量给出可定位段落。
{% else %}
## 定位严格度（balanced）
- 在覆盖率与可定位性之间平衡；优先给出可落段问题。
{% endif %}

## 审计结果字段要求
- `issues`：只放可验证、可定位、可交接的问题；每项字段含义以上文“问题字段语义”和“定位与修复输出要求”为准。
- `repair_plan`：修复队列的候选排序；每项包含 chapter_number、issue_ids、priority、strategy，不表示已修改正文。
- `summary`：概述本批次整体一致性风险，低价值重复项可在此合并说明。
- `consistency_score`：0-10 分，按问题严重度和覆盖范围扣分；没有明确问题时保持高分。

## 质量校准
- 工作身份：你是一位严谨的小说质量审校员，擅长用短证据、明确边界和下游可执行建议定位问题。
- 审校路径：先确认输入证据，再判断问题类型、影响范围和修复边界，最后给下游可执行的定位信息。
- 字段级质量示例（非输出格式）：平弱=“这里不连贯”；高质量=“指出哪两处证据冲突、风险等级、可验证问题”。
- 可操作路径：每条判断都要能支持复核或修复，低置信度内容降级为待验证问题。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否给出短证据、风险等级、定位信息和下游可验证的问题。

{#
================================================================================
【备注】
================================================================================
提示词：extract_knowledge_deltas.j2
服务步骤：EXTRACT_KNOWLEDGE_DELTAS（知识增量提取）
作用简述：从章节正文中提取角色新获得或揭示的知识，输出结构化 knowledge_deltas。
上游输入：
  - chapter_number：当前章节编号
  - chapter_text：章节正文
  - pov_character：当前 POV 角色（可选）
  - existing_knowledge_summaries：已有知识摘要列表，用于去重
下游输出：
  - JSON：knowledge_deltas 列表
  - 下游消费者：finalize.py → StoryKernel.knowledge_ledger 追加
消费链优先级：P0（entity_id + fact 准确）> P1（knowledge_type 分类）> P2（visibility 判定）
================================================================================
#}

{#- ---------------------------------------------------------------------------
    指导层：任务定义 + 上下文消费
    --------------------------------------------------------------------------- -#}

你是长篇小说的知识增量提取员。你的任务是从章节正文中提取角色在本章**新获得**或**新揭示**的知识。

## 当前章节
第 {{ chapter_number }} 章
{% if pov_character %}
## 当前 POV 角色
{{ pov_character }}
{% endif %}

## 提取目标

### 1. 新知识获取
角色在本章中通过以下方式获得的新知识：
- **直接观察**：角色亲眼看到、亲耳听到
- **被告知**：其他角色主动告知
- **偷听/发现**：角色无意中获知
- **推理得出**：角色根据线索推断

### 2. 知识揭示
原本保密的知识在本章被其他角色获知（秘密暴露）。

## 已有知识摘要（避免重复）
{% if existing_knowledge_summaries %}
以下知识已存在于知识账本中，**不要重复提取**：
{% for summary in existing_knowledge_summaries %}
- {{ summary }}
{% endfor %}
{% else %}
（无已有知识记录）
{% endif %}

## 字段说明

将本章新增知识汇总到顶层 `knowledge_deltas` 列表；如果没有新增知识，保持列表为空，不要编造。

### entity_id
知识所属角色的实体 ID。格式为 `char_<角色名>`，例如 `char_李明`、`char_张伟`。
如果正文中使用的是角色全名或常用称呼，请保持一致。

### fact
角色获得的具体知识内容。用一句话简洁描述，不超过 100 字。
- 描述角色**知道了什么**，而非角色**做了什么**
- 示例：「李明知道了张伟就是幕后黑手」而非「李明跟踪了张伟」

### knowledge_type
知识类型，取值为：
- `known`：角色确认知道的事实
- `suspected`：角色怀疑但尚未确认
- `misbelief`：角色的错误认知
- `secret_kept`：角色知道但对外保密的信息

### visibility
知识的可见性：
- `public`：多人知晓的公开信息
- `private`：仅该角色知道的私密信息
- `secret`：刻意隐瞒的秘密

### source_chapter
知识获得的章节编号，即当前章节 {{ chapter_number }}。

## 提取规则
1. **只提取本章新增知识**：不要提取角色在之前章节已经知道的内容
2. **避免重复**：对照已有知识摘要，跳过已记录的知识
3. **精确归属**：每条知识必须明确是哪个角色获得的
4. **区分类型**：准确区分 known/suspected/misbelief/secret_kept
5. **合理判定可见性**：
   - 如果信息在场景中多人可见 → `public`
   - 如果仅 POV 角色或单个角色获知 → `private`
   - 如果角色刻意隐瞒 → `secret`
6. **宁缺毋滥**：如果本章没有明显的新知识获取，返回空列表即可

## 章节正文

{{ chapter_text }}

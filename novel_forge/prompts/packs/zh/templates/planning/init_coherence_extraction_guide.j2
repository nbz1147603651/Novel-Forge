{#
================================================================================
【备注】
================================================================================
提示词：init_coherence_extraction_guide.j2
服务步骤：INIT_COHERENCE_EXTRACTION_GUIDE（一致性画像分片：抽取指南）
作用简述：只生成后续 claims 抽取应关注的规则，避免画像精炼输出过大。
上游输入：current_profile/spec/story_bible/character_bible/blueprint
下游输出：JSON：{"extraction_guidance": [...]}
下游消费者：REFINE_INIT_COHERENCE_PROFILE 本地合并器
消费链优先级：P0 = 指导可执行；P1 = 指向状态/关系/承诺/知识；P2 = 简短
================================================================================
#}
{#- 指导层 -#}
请只输出 claims 抽取指南，每条都是后续抽取器可以执行的判断规则。

## 输出字段说明
- `extraction_guidance`：抽取指南列表，每条为一条可执行的判断规则，指向状态/关系/承诺/知识等维度的关注点。
- 列表项必须是短字符串，不得输出对象、章节剧情复述或角色小传。
- 指南使用“主角/知情角色/关系双方”等角色类型，不写任何具体人物姓名；具体姓名只能由后续 Claims 当前源分块提供。

{% if shared_evidence_anchor %}
## 共享证据锚点
{{ shared_evidence_anchor | tojson(indent=2) }}
请以此作为本次分片的共同依据；维度特需信息只能补充，不得覆盖锚点事实。
{% endif %}

## 当前画像
{{ current_profile | tojson(indent=2) }}

## 项目本体、兑现规则与冲突维度
{{ project_ontology | tojson(indent=2) }}
{{ payoff_rules | tojson(indent=2) }}
{{ conflict_lens | tojson(indent=2) }}

## 规范角色名册（只用于确认边界，不得抄入指南）
{{ canonical_character_roster | default([]) | tojson(indent=2) }}

## 质量校准
- 工作身份：你是一位长篇叙事架构师，擅长把故事资产拆成可承接、可验证、不过度剧透的执行计划。
- 规划推断路径：先锁定 P0 硬约束，再分配 P1 推进节奏，最后用 P2 风格/主题补足表现层。
- 字段级质量示例（非输出格式）：平弱=“推进关系”；高质量=“写清谁因何行动、关系如何变化、下一环如何承接”。
- 可操作路径：计划项要能被后续写作/审计直接检查，避免只给抽象愿望或主题口号。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否把抽象目标转化为可观察的行动、冲突、状态变化或章节承接点。

{#- 格式层 -#}

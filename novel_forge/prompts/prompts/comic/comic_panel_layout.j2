{#
================================================================================
【备注】
================================================================================
提示词：comic_panel_layout.j2
服务步骤：COMIC_PANEL_LAYOUT（漫画管线：页/格节奏规划）
作用简述：基于剧本场次清单给出每页（Track）分格数（Clip 数量）与节奏
         说明，只做节奏判断，不改写对白、不新增剧情。只输出部分对象
         （PARTIAL_OBJECT）：{"pages":[...]}。
上游输入：
  - title/format: 项目标题与漫画形态（page=页漫 / webtoon=条漫）
  - aspect/page_budget_low/page_budget_high: 画幅与每页分格硬预算
  - scenes: 场次清单（scene_id/heading/对白数/动作数），来自成稿回流投影
下游输出：
  - JSON（PARTIAL_OBJECT）：{"pages":[{"page_number":1,"panel_count":6,
    "rhythm_note":"..."}], "notes":"..."}
  - 下游消费者：
    * comic/layout.py::plan_comic_pages：按 panel_count 目标装填分格
    * comic/pipeline.py::audit_comic_layout：硬预算门禁复核
消费链优先级：
  P0 = 硬预算：panel_count 必须落在 [page_budget_low, page_budget_high]
  P1 = 节奏：对白密集场次分格偏多、动作高潮给大格（页尾/条尾）
  P2 = 连续性：page_number 从 1 连续递增，总格量覆盖全部场次节拍
简要例子：
  输入：{"format":"page","page_budget_low":5,"page_budget_high":7,"scenes":[{"scene_id":"sc01","dialogue_count":6,"action_count":2}]}
  输出片段：pages=[{"page_number":1,"panel_count":6,"rhythm_note":"对白密集，紧凑六格"}]
================================================================================
#}
{#- ---------------------------------------------------------------------------
    指导层：任务与输入
    --------------------------------------------------------------------------- -#}

你是漫画责编，负责把已锁定的剧本场次规划成页/格节奏。只做节奏判断：决定每一页放几格、节奏如何起伏；不得改写对白，不得新增或删减剧情。

## 项目信息
- 标题：{{ title }}
- 形态：{{ format }}（page=页漫横版页，webtoon=条漫竖滑条）
- 画幅：{{ aspect }}
- 每页分格硬预算：{{ page_budget_low }}-{{ page_budget_high }} 格

## 场次清单（来自成稿回流投影，只读）
{{ scenes | tojson(ensure_ascii=False) }}

{#- ---------------------------------------------------------------------------
    约束层：质量与边界
    --------------------------------------------------------------------------- -#}

## 分页规则
- **硬预算**：`pages` 每项含 `page_number`、`panel_count`、`rhythm_note`；`panel_count` 必须是 {{ page_budget_low }}-{{ page_budget_high }} 之间的整数，越界视为无效。
- **连续性**：`page_number` 从 1 开始连续递增，不跳号；总格量应足以覆盖全部场次节拍（建立镜头 + 对白/动作节拍）。
- **节奏**：对白密集场次给更多格、单格信息量低；动作高潮与悬念收尾安排在页尾/条尾大格，`rhythm_note` 用一句话说明该页节奏意图。
- **忠实度**：只消费给定场次清单，禁止虚构未列出的场次或角色。
- **JSON 稳定性**：只输出符合 Schema 的对象；整数字段必须是数字而非字符串；不要输出 Markdown、注释或省略号。
- 输出顶层必须包含 `pages` 键（数组），可附 `notes` 说明整体节奏策略。

{#
================================================================================
【备注】
================================================================================
提示词：extract_chapter_summary_exit.j2
服务步骤：EXTRACT_CHAPTER_SUMMARY_EXIT（Canon 分片：摘要与出口状态）
作用简述：先提取章节事实摘要和出口状态，作为其他 Canon 分片的共同锚点。
上游输入：chapter_number/chapter_text/chapter_outline_summary/known_characters
下游输出：JSON：{"chapter_exit_state", "structured_summary"}
下游消费者：EXTRACT_CANON 本地合并器及后续 Canon 分片
消费链优先级：P0 = 只写正文可证实事实；P1 = 下一章承接必需；P2 = 控制长度
================================================================================
#}
{#- 指导层 -#}
请只提取本章的事实摘要与章末出口状态。不要输出 canon_delta、关系变化或情节线变化。

## 事实来源边界（P0）
- 只从 FinalArtifact 对应的最终正文 / `chapter_text` 提取事实摘要和章末出口状态。
- 大纲目标、known_characters、权威性别只用于识别和规范化，不是已发生事实来源。
- 计划、章节契约、中间稿、source 投影中的未发生内容不得写入 structured_summary 或 chapter_exit_state。
- 没有正文证据的章末状态、角色去向、开放问题或下一章承接点不得写入。

{% if shared_evidence_anchor %}
## 共享证据锚点
{{ shared_evidence_anchor | tojson(indent=2) }}
请以此作为本次分片的共同依据；维度特需信息只能补充，不得覆盖锚点事实。
{% endif %}

## 章节
- 第 {{ chapter_number }} 章
{% if chapter_outline_summary %}- 大纲目标：{{ chapter_outline_summary }}{% endif %}

## 当前已知角色
{% for name in known_characters %}
- {{ name }}
{% endfor %}

{% if authoritative_character_genders %}
## 角色权威性别（不得改写）
{% for name, gender in authoritative_character_genders.items() %}
- {{ name }}：{{ gender }}
{% endfor %}
{% endif %}

## 正文
{{ chapter_text }}

## 要求
- structured_summary 用 1 段话概括本章已发生事实。
- chapter_exit_state 只写章末真实状态和下一章承接必需信息。
- chapter_exit_state.must_carry_forward 只能写叙事事实，不要写元指令；每项可写为字符串（默认 open）或对象 `{"text":"叙事事实","status":"open|abandoned|deferred","abandon_reason":"放弃理由","deferred_to_chapter":章节号}`。仅当某条遗留确实不打算在本弧线承接时才标 abandoned 并填理由；明确推迟到某章承接时标 deferred 并填章节号；其余一律 open，禁止滥用 abandoned 逃避承接。
- character_end_states 最多 {{ max_exit_state_characters | default(6) }} 人。

## 质量校准
- 工作身份：你是一位证据优先的叙事事实抽取员，擅长从正文和上游锚点中提取可追溯、可合并的状态信息。
- 抽取路径：先找正文中的明确事实，再对照共享锚点与已知状态，最后只保留能被后续合并器消费的信息。
- 证据校准：同一事实已有上游分片确认时保持一致；证据不足时用保守字段值表达未知或待确认。
- 可操作路径：每条信息都服务于 canon、memory、state 或审计链路，避免把语气、比喻、猜测当作事实。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否保留后续合并/检索需要的事实边界，且没有把推测写成既定事实。

{#- 格式层 -#}

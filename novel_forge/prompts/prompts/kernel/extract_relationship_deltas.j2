{#
================================================================================
【备注】
================================================================================
提示词：extract_relationship_deltas.j2
服务步骤：EXTRACT_RELATIONSHIP_DELTAS（Canon 分片：关系变化）
作用简述：只提取本章明确的人物关系变化，并以前态关系和章节摘要为锚点。
上游输入：chapter_number/chapter_text/summary_exit/prior_relationships/known_characters
下游输出：JSON：{"relationship_deltas": [...]}
下游消费者：EXTRACT_CANON 本地合并器、Canon relationship tracker
消费链优先级：P0 = 只写人物关系；P1 = trust/tension 合理；P2 = 不新增概念关系
================================================================================
#}
{#- 指导层 -#}
请只提取人物关系变化。没有明确变化时返回空数组。

## 事实来源边界（P0）
- 只从 FinalArtifact 对应的最终正文 / `chapter_text` 提取关系变化。
- 前态关系、summary_exit、共享证据锚点只用于判断“是否变化”和复用 ID，不是新增事实来源。
- 没有正文证据的关系升降、确认、和解、背叛或身份关系不得写入。

## 输出字段说明
- `relationship_deltas`：关系变化列表，每条包含角色对（characters 数组）、关系类型、trust/tension/dependency 变化（0.0-1.0）、正文证据。

{% if shared_evidence_anchor %}
## 共享证据锚点
{{ shared_evidence_anchor | tojson(indent=2) }}
请以此作为本次分片的共同依据；维度特需信息只能补充，不得覆盖锚点事实。
{% endif %}

## 章节锚点
- 第 {{ chapter_number }} 章
{% if summary_exit %}{{ summary_exit | tojson(indent=2) }}{% endif %}

## 已知角色
{% for name in known_characters %}- {{ name }}{% endfor %}

{% if prior_relationships %}
## 前态关系
{% for rel in prior_relationships %}
- {{ rel.pair_id }}：{{ rel.public_status }}（信任={{ rel.trust }}，张力={{ rel.tension }}）
{% endfor %}
{% endif %}

{% if character_state_deltas %}
## 已确定角色状态变化（关系变化必须由这些状态或正文事件支撑）
{{ character_state_deltas | tojson(indent=2) }}
{% endif %}

## 正文
{{ chapter_text }}

## 要求
- 最多 {{ max_relationship_deltas | default(8) }} 条。
- relationship.characters 必须是两个角色名数组，且每个值必须直接来自“已知角色”或正文中明确出现的人名。
- 禁止把 `character_id`、`name`、`gender`、`status` 等结构字段或“镇民/众人/百姓/官兵”等群体称谓写入 relationship.characters。
- `public_status` 只能写短标签（如“母子”“师徒”“盟友”“敌对”“朋友”），不得写整段关系描述。
- `change_summary` 必须概括本章的明确互动事件；“未直接出现互动”“无明确变化”“本章未提及”等无变化说明不得作为 delta 输出，应返回空数组。
- trust/tension/dependency 为本章结束后的绝对状态值（0.0-1.0），不是变化量。

## trust / tension / dependency 校准尺（P1）
- 先匹配“前态关系”中的同一角色对；若无前态，默认 trust=0.5、tension=0.5、dependency=0.0。
- 没有正文证据时不得为了“看起来有变化”改数值；如果只是复述既有关系且没有新事件，返回空数组。
- trust 上升证据：保护、兑现承诺、主动坦白、共享秘密、承担代价、在压力下选择合作。
- trust 下降证据：欺骗、背叛、抛弃、胁迫、公开指控、隐瞒造成可见后果。
- tension 上升证据：威胁、冲突升级、互相试探、未解误会、利益对撞、公开敌意。
- tension 下降证据：和解、误会澄清、共同完成行动、让步、危机后形成稳定协作。
- dependency 表示双方在行动、信息、资源或情感上的相互依赖；只有正文显示“离不开/必须借助/被对方牵制”时才升高。
- 变化幅度：语气或短对白 ≤0.05；明确行动后果 0.10-0.15；重大救援/背叛/公开决裂 0.20-0.30；超过 0.35 必须有强正文证据，并填写 last_shift_event / change_summary。
- trust 和 tension 可同时高：例如“高度互信但处于高压冲突中”。不要把 tension 自动当作 trust 的反向值。
- 所有数值都必须基于前态加减后截断到 0.0-1.0；不确定时保守沿用前态。

## 质量校准
- 工作身份：你是一位证据优先的叙事事实抽取员，擅长从正文和上游锚点中提取可追溯、可合并的状态信息。
- 抽取路径：先找正文中的明确事实，再对照共享锚点与已知状态，最后只保留能被后续合并器消费的信息。
- 证据校准：同一事实已有上游分片确认时保持一致；证据不足时用保守字段值表达未知或待确认。
- 可操作路径：每条信息都服务于 canon、memory、state 或审计链路，避免把语气、比喻、猜测当作事实。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否保留后续合并/检索需要的事实边界，且没有把推测写成既定事实。

{#- 格式层 -#}

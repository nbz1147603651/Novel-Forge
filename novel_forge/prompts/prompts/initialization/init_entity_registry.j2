{#
================================================================================
【备注】
================================================================================
提示词：init_entity_registry.j2
服务步骤：INIT_ENTITY_REGISTRY（实体注册表初始化）
作用简述：从项目初始化资料识别角色、地点、物品、组织和概念实体，建立稳定实体注册表。
上游输入：
  - spec：故事规格
  - story_bible：世界观设定
  - character_bible：角色设定
  - character_system：已确认的人物关系系统
  - existing_entity_registry：结构化基线目录（unknown 表示待 LLM 分类）
  - entity_registry_scope：本轮补充范围
下游输出：
  - JSON：entities 列表，包含 entity_id, name, entity_type, aliases, source, notes
  - 下游消费者：候选状态抽取 / 叙事状态裁判
消费链优先级：P0（稳定 entity_id/name/type）> P1（aliases）> P2（notes）
================================================================================
#}

{#- 指导层 -#}
## 任务
从项目初始化资料中建立实体注册表。你只负责识别实体和可能别名，不做章节事实裁判。

输出控制：
- 优先输出角色、核心地点、关键物件、组织和核心概念；不要罗列普通场景道具。
- `notes` 保持空字符串或 20 字以内短句，禁止展开背景说明。
- `entity_id` 必须稳定、短小；同一实体不要重复输出。
- 本产物是后续 source artifacts 的 canonical entity registry；角色、地点、物件、组织、概念必须在这里获得唯一 `entity_id`。
- 旧名、误称、前世名、绰号、组织内称谓只能进入同一实体的 `aliases`；不得另建同义实体。
- 若别名没有在输入资料中明确出现，不要自行发明；不确定的称呼写入 `notes` 的短风险提示，而不是注册为正式 alias。
- 后续章节提示词只会引用 `entity_id` 或本注册表投影；因此 `name` 必须是权威显示名，禁止混入阶段状态或剧情解释。

## 字段内容约束
- 每个实体对象只能包含 `entity_id`、`name`、`entity_type`、`aliases`、`source`、`notes`。
- `entity_type` 只能是 `character`、`location`、`item`、`organization`、`concept`、`unknown`。
- 字段名只使用上方列出的英文 `snake_case` key；分类、背景、关系或章节事实说明并入 `notes`。

## 输入
故事规格：
{{ spec }}

世界观：
{{ story_bible }}

角色设定：
{{ character_bible }}

{% if existing_entity_registry | default({}) %}
## 结构化基线实体目录
这是固定事实源的当前快照，不会写入向量库。已有明确类型的实体不得改名、改 ID 或重复输出；
`entity_type=unknown` 的实体是本步骤必须结合输入语义重新分类的待决项，输出时沿用其 `entity_id` 与 `name`。
{{ existing_entity_registry }}
{% endif %}

{% if entity_registry_scope | default({}) %}
## 本轮补充范围
{{ entity_registry_scope }}
{% endif %}

{% if character_system | default({}) %}
## 人物关系系统
仅用于识别关系描述中提到的非人物目标；不要重复输出人物实体。
{{ character_system }}
{% endif %}

## 质量校准
- 工作身份：你是一位长篇叙事架构师，擅长把故事资产拆成可承接、可验证、不过度剧透的执行计划。
- 规划推断路径：先锁定 P0 硬约束，再分配 P1 推进节奏，最后用 P2 风格/主题补足表现层。
- 字段级质量示例（非输出格式）：平弱=“推进关系”；高质量=“写清谁因何行动、关系如何变化、下一环如何承接”。
- 可操作路径：计划项要能被后续写作/审计直接检查，避免只给抽象愿望或主题口号。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否把抽象目标转化为可观察的行动、冲突、状态变化或章节承接点。

{#- 格式层 -#}

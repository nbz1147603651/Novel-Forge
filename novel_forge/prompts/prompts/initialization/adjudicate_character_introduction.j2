{#
================================================================================
【备注】
================================================================================
提示词：adjudicate_character_introduction.j2
服务步骤：ADJUDICATE_CHARACTER_INTRODUCTION（新角色建档裁决）
作用简述：在自动建档前，判断结构化候选是否真是值得建档的新角色、已有角色别名或临时叙事标签
上游输入：
  - chapter_number: 当前章节号
  - existing_characters: 已有角色列表
  - candidate_records: 本地从章节大纲、章节契约、蓝图收集的候选与来源
  - chapter_outline: 当前章节大纲
  - chapter_contract: 当前章节契约
  - blueprint_context: 当前章节相关蓝图片段
下游输出：
  - JSON：decisions, summary
  - 下游消费者：introduce_new_characters / CharacterBible
消费链优先级：
  P0 = 不把已有角色别名、阶段状态、职业/功能标签、群体称谓建成新人
  P1 = 对真正影响剧情推进、关系状态或后续承接的新人给出建档许可
  P2 = 给出 canonical_name、importance、reason 便于审计
简要例子：
  输入：candidate_records=[{"name":"周芷若（狱中）","sources":["chapter_contract"]}]
  输出：{"decisions":[{"candidate_name":"周芷若（狱中）","canonical_name":"周芷若","verdict":"merge_existing",...}],"summary":"..."}
================================================================================
#}
{#- ---------------------------------------------------------------------------
    指导层：任务与输入
    --------------------------------------------------------------------------- -#}

你是一位小说连续性编辑。你的任务不是生成角色档案，而是在自动建档之前做一次语义裁决：
判断每个候选名字究竟是“应写入 CharacterBible 的新角色”，还是“已有角色别名/阶段状态/伪装称呼/职业标签/群体称谓/道具或知识项”。

## 当前章节
第 {{ chapter_number }} 章

## 已有角色
{% if existing_characters %}
{% for ch in existing_characters %}
- {{ ch.name }}（{{ ch.role }}）{% if ch.notes %}：{{ ch.notes }}{% endif %}
{% endfor %}
{% else %}
- 暂无
{% endif %}

## 候选记录
候选来自本地结构化来源，不等于裁决结论。请逐项判断。
{{ candidate_records }}

## 当前章节大纲
{{ chapter_outline }}

{% if chapter_contract %}
## 当前章节契约
{{ chapter_contract }}
{% endif %}

{% if blueprint_context %}
## 当前章节相关蓝图片段
{{ blueprint_context }}
{% endif %}

## 裁决原则
1. 只有“具体、可追踪、具有独立叙事身份的人物”才可 `create_profile`。
2. 已有角色的阶段状态、处境括注、伪装称呼、视角误认、前世/今生标签，应输出 `merge_existing` 或 `skip`，不要建成新人。
3. 职业/功能/群体/组织/道具/知识/事件标签，应输出 `skip`。
4. 官职/职能称谓（如“黄门侍郎”“店员”“侍卫”“信使”）只有在输入中已经给出可追踪的规范姓名时才可 `create_profile`；不要为了建档临时编姓名。
5. 如果候选是尚未建档但后续明显会复现、承担关系变化、推动主线/支线或作为重要对手/盟友出现，可 `create_profile`。
6. 如果只是本章一次性路人、店员、旁观者、工具人，输出 `skip` 或 `defer`。
7. `canonical_name` 必须是最终应进入角色设定的名字；若是已有角色别名，填写已有角色名，并在 `matched_existing_name` 中重复该名字。
8. `importance` 只能是 major / supporting / minor / incidental；只有 major、supporting、minor 且 `should_create_profile=true` 时才会进入建档候选。

## 字段内容约束
- 每个 `decisions[]` 对象只能包含 `candidate_name`、`canonical_name`、`verdict`、`importance`、`should_create_profile`、`matched_existing_name`、`confidence`、`reason`、`evidence`。
- 不要新增分析字段、分组字段、角色档案字段或候选外结论；裁决理由只写入 `reason` 和 `evidence`。

## 质量校准
- 工作身份：你是一位严谨的小说质量审校员，擅长用短证据、明确边界和下游可执行建议定位问题。
- 审校路径：先确认输入证据，再判断问题类型、影响范围和修复边界，最后给下游可执行的定位信息。
- 字段级质量示例（非输出格式）：平弱=“这里不连贯”；高质量=“指出哪两处证据冲突、风险等级、可验证问题”。
- 可操作路径：每条判断都要能支持复核或修复，低置信度内容降级为待验证问题。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否给出短证据、风险等级、定位信息和下游可验证的问题。

{#- ---------------------------------------------------------------------------
    系统契约由 PromptBuilder 注入
    --------------------------------------------------------------------------- -#}



## 字段取值约束
- `verdict` 只能是 create_profile / merge_existing / skip / defer。
- `importance` 只能是 major / supporting / minor / incidental。
- `confidence` 为 0.0 到 1.0 的数字。
- `decisions` 必须覆盖输入的每一个候选。

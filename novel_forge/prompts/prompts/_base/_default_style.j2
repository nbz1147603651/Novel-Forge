{#- 默认写作风格 - 通用基础风格 -#}
{#- Usage: {% include "_base/_default_style.j2" %} -#}
{#- 此风格是原始的通用风格，适合所有类型的写作 -#}

{#-
  === 宏模块：默认写作风格（通用基础风格）===
  文件类型：宏模块（macro module）
  作用简述：定义通用基础写作风格约束和执行提醒，作为默认风格被其他风格模板继承或覆盖。

  定义的宏及参数：
  - 无宏定义，此文件为直接渲染型模板（使用 {% include %} 而非 {% import %}）

  使用方式：
  - 作为通用基础风格注入，被写作模板通过 {% include "_base/_default_style.j2" %} 引入。体裁特化风格规则由 PROFILE_STYLE 步骤动态推导。
-#}

## 执行提醒
- 对话是信息、关系和决策的主要载体，但不要为了比例硬堆对话。
- 计划中的 `relationship_dynamics`、`emotional_beat` 要转译成动作、对白和选择；`sensory_notes` 是候选锚点，不是逐条兑现清单。
- 每场优先选择 1-2 个最能服务冲突的细节，已有动作/对白足以承载信息时，不额外补感官描写。
- 技术/设定信息优先借人物对话、动作和必要感官自然带出，避免长段旁白讲解。

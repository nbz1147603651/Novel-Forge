{#
================================================================================
【备注】
================================================================================
提示词：repair_knowledge_boundary.j2
服务步骤：REPAIR_KNOWLEDGE_BOUNDARY（知识边界修复）
作用简述：定向改写窗口文本中 POV 角色不应知道的越界认知，保持戏剧意图但
          移除全知信息。仅改写违规段落 ± 2 段窗口。
上游输入：
  - chapter_number：章节号
  - pov_character：当前 POV 角色
  - violations：越界违规列表（evidence_quote / reason / severity / paragraph_start / paragraph_end / repair_goal）
  - window_text：违规段落 ± 2 段的窗口正文
下游输出：
  - JSON：repaired_text（修复后窗口正文）+ changes（变更描述列表）
消费链优先级：
  P0 = 移除或改写越界认知
  P1 = 保持戏剧张力和叙事节奏
  P2 = 不引入新的越界信息
================================================================================
#}
{%- from "_base/_cognitive_constraints.j2" import render_cognitive_constraints -%}

{#- ---------------------------------------------------------------------------
    指导层
    --------------------------------------------------------------------------- -#}

你是一位资深小说编辑，负责修复第 {{ chapter_number }} 章中的**知识边界越界**问题。

## 当前 POV 角色

**{{ pov_character or '未指定' }}**

{% if cognitive_constraints | default([]) %}
## 可执行认知边界（P0）
- 修复结果必须回到下列契约，不得仅删除某一泄露句却让同窗口内其他句继续越界。
{{ render_cognitive_constraints(cognitive_constraints) }}
{% endif %}

## 越界违规

{% for v in violations %}
### 违规 {{ loop.index }}
- **严重程度**：{{ v.severity }}
- **证据原文**：「{{ v.evidence_quote }}」
- **越界原因**：{{ v.reason }}
- **段落范围**：第 {{ v.paragraph_start }}–{{ v.paragraph_end }} 段
{% if v.repair_goal %}- **修复目标**：{{ v.repair_goal }}{% endif %}
{% endfor %}

## 待修复窗口正文

以下是违规段落及其上下文（± 2 段），你需要在这个窗口内进行定向改写：

---
{{ window_text }}
---

## 修复规则

1. **精准定位**：找到窗口正文中包含越界认知的具体句子或短语——即 POV 角色不应知道的信息。
2. **最小改写**：只改写越界的部分，保留周围段落的原始内容和叙事节奏。
3. **认知约束**：改写后的文字只能包含 POV 角色通过以下方式可以获得的信息：
   - 直接观察（视觉、听觉、嗅觉、触觉等感官感知）
   - 与其他角色的对话交流
   - 角色自身的回忆和推理（基于已知事实的合理推断）
   - 当前章节已经发生的事件
4. **保持戏剧意图**：如果越界信息服务于某个戏剧效果（如悬念、反转铺垫），改写时应保留该效果，但通过 POV 角色合理的感知途径来实现。
5. **不引入新越界**：改写过程中不得引入新的越界认知或全知视角信息。
6. **风格一致**：改写后的文字应与窗口内其他段落保持相同的文风、语调和叙事节奏。
7. **字数控制**：改写后的窗口正文字数应与原文相差不超过 ±30%。

{% if repair_goal %}
## 特别修复指引

{{ repair_goal }}
{% endif %}

## 输出要求

返回修复后的完整窗口正文（替换原来的窗口内容），并列出每处变更的段落范围和简要说明。
- `repaired_text` 表示可直接替换原窗口的完整修复后正文，不是片段摘要。
- `changes` 记录每处变更的段落范围、原问题和修复意图，便于复查。

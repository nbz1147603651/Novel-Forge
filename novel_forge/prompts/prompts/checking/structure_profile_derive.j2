{# ================================================================================
【备注】
================================================================================
提示词：structure_profile_derive.j2
服务步骤：PROFILE_STRUCTURE（结构规范推导）
作用简述：专注推导叙事结构相关的四个核心配置（钩子/情节线/微兑现/爽点）。
上游输入：
  - genre：题材
  - tone：基调
  - narrative_complexity：叙事复杂度
  - blueprint_elements：已选叙事要素与优先级
下游输出：
  - JSON：{"hook_config", "strand_config", "micro_payoff_config", "cool_point_config"}
  - 下游消费者：StyleProfile / PlanStep / EvaluateStep
消费链优先级：P2（风格建议，供规划时参考）
简要例子：
  输入：genre="玄幻", tone="热血", narrative_complexity="complex"
  输出：{"hook_config": {"preferred_types": ["crisis"], "strength_baseline": "strong"}, "strand_config": {...}}
================================================================================
#}

{#- ---------------------------------------------------------------------------
    指导层：任务定义 + 上下文消费
    --------------------------------------------------------------------------- -#}
你是一位资深的小说结构设计顾问。请根据以下项目基本信息和已选叙事要素，**专注推导**结构参数配置。

## 任务目标
仅推导与叙事结构相关的四个核心配置：
- hook_config（章尾钩子）
- strand_config（情节线阈值）
- micro_payoff_config（微兑现）
- cool_point_config（爽点）

**不涉及**：modules（写作技法规范）、global_style（对话/节奏/情绪风格）、summary（风格定位）、banned_phrases（禁用短语）

## 作品基本信息
- 题材：{{ genre }}
- 基调：{{ tone }}
- 叙事复杂度：{{ narrative_complexity }}

{% if synopsis %}
## 故事梗概（结构推导的核心依据）

{{ synopsis }}
{% endif %}

{% if premise and not synopsis %}
## 故事前提（结构推导的核心依据）

{{ premise }}
{% endif %}

{% set _be = blueprint_elements or {} %}
{% set _be_ext = _be.extension_elements if _be.extension_elements is defined else [] %}
{% set _be_constraints = _be.focus_constraints if _be.focus_constraints is defined else [] %}
{% set _be_summary = _be.selector_summary if _be.selector_summary is defined else "" %}
{% if _be_ext or _be_constraints %}
## 已选叙事要素（结构推导必须消费）

{% if _be_summary %}- 选择结论：{{ _be_summary }}{% endif %}
{% if _be_constraints %}
- 执行约束：
{% for item in _be_constraints %}
  - {{ item }}
{% endfor %}
{% endif %}
{% if _be_ext %}
- 扩展机制：
{% for item in _be_ext %}
  - {{ item.name }}（{{ item.element_id }} / {{ item.category }}{% if item.selection_score is defined and item.selection_score %} / 优先级 {{ item.selection_score }}{% endif %}{% if item.selection_source is defined and item.selection_source %} / 来源 {{ item.selection_source }}{% endif %}）：{{ item.selection_reason }}；结构提示：{{ item.prompt_hint }}
{% endfor %}
{% endif %}

**转译要求**：请优先消费高优先级机制，把它们映射到钩子类型、情节线断档阈值、微兑现类型和爽点模式。不要把要素名称直接输出为爽点模式；必须转成结构参数。
{% endif %}

---

## 推导逻辑说明

**重要原则**：以下推导表仅为参考基准，你必须**结合故事梗概中的具体冲突、角色目标、叙事结构和已选叙事要素**进行个性化推导。同题材的不同故事可能有截然不同的结构需求。

### hook_config（章尾钩子）

**钩子类型限制**：只能使用 `crisis/mystery/emotion/choice/desire`

**功能基准**：
| 叙事信号 | 候选钩子类型 | 强度判断 |
|------------|-----------------|-------------|
| 外部威胁、损失倒计时、局势失控 | crisis | 威胁迫近且不可延后时 strong |
| 信息缺口、身份遮蔽、规则未明、证据链待补 | mystery | 缺口直接影响下一步行动时 strong |
| 关系裂缝、亲密推进、信任考验、情绪反噬 | emotion | 情绪会改变选择时 medium/strong |
| 阵营、代价、底线、牺牲交换 | choice | 选择不可两全时 strong |
| 目标受阻、认可需求、资源追求、身份欲望 | desire | 欲望与阻力正面碰撞时 medium/strong |

**故事特异性推导要求**：
- 根据故事梗概中下一章最想让读者追问的问题来选类型；不要只按题材标签套默认组合。
- 如果已选要素提供了证据、规则、循环差异或调查流程，优先考虑 `mystery/choice`，但必须写清它如何影响行动。
- 如果已选要素提供了关系契约、潜台词、仪式、家庭/组织压力，优先考虑 `emotion/choice/desire`，但不得把纯氛围当钩子。
- 如果已选要素提供了资源循环、市场/组织反馈、能力成长或补给压力，优先考虑 `desire/crisis/choice`，并体现可见代价。

### strand_config（情节线阈值）

**参考基准（根据叙事复杂度）**：

| 复杂度 | quest_max_consecutive | fire_max_absent | constellation_max_absent | stagnation_threshold |
|--------|----------------------|-----------------|------------------------|---------------------|
| simple | 3 | 15 | 20 | 4 |
| standard | 5 | 10 | 15 | 3 |
| complex | 3 | 6 | 10 | 2 |
| epic | 2 | 4 | 8 | 2 |

**故事特异性调整要求**：
- 如果故事梗概中有多条并行情节线（如主线任务+感情线+势力博弈）→ 在上述基准上收紧阈值（各减1-2）
- 如果故事梗概以单线推进为主（如纯解谜、纯升级）→ 可适当放宽断档阈值
- 如果已选要素超过4个且横跨关系、资源、权力、线索等多个类别 → 视为复合结构，至少将 `fire_max_absent` 和 `constellation_max_absent` 各收紧1

### micro_payoff_config（微兑现）

**微兑现类型限制**：只能使用 `information/relationship/ability/resource/recognition/emotion/clue`

**功能基准**：
| 主角阶段追求 | 候选微兑现类型 |
|------------|-------------------|
| 获取事实、规则、证据或局势判断 | information, clue |
| 改变关系距离、信任、承诺或情绪认知 | relationship, emotion |
| 获得技能、权限、方法、身份能力或行动资格 | ability, recognition |
| 获得物资、机会、组织支持、交易筹码或生存条件 | resource, recognition |

**故事特异性推导要求**：
- 分析故事梗概中**主角的核心追求**：是获取信息/真相、提升行动能力/身份、修复或重塑关系，还是获取资源/机会？
- 微兑现类型必须与主角的核心追求直接对应
- 如果故事梗概中存在"发现秘密→能力成长→关系变化"的复合结构 → 可混合设置，但须明确主次
- 如果已选要素包含线索/文书/调查/规则揭示 → `information/clue` 不能缺席
- 如果已选要素包含关系契约/潜台词/治愈仪式/家族压力 → `relationship/emotion` 不能缺席
- 如果已选要素包含资源循环/市场反馈/补给/经营 → `resource/recognition` 不能缺席

**每章最低微兑现数量**：根据叙事复杂度，simple=0, standard=1, complex=2, epic=2
- 如果故事梗概中信息、线索、规则或资源节点密集，即使复杂度为 standard，也可提升至 2

### cool_point_config（爽点）

**功能基准**：
爽点模式必须从本作的“优势如何变成可见结果”推导，可来自：
- 信息优势：提前看见真相、识破伪装、连接证据、利用规则差。
- 行动优势：以更高代价承受力、更精确执行或新能力解决阻碍。
- 关系优势：信任被兑现、误解被推进到新选择、情感压力转化为行动。
- 资源优势：把筹码、身份、组织、物件或机会转化为局势变化。
- 价值优势：角色坚持底线或改变信念后获得读者认可。

**故事特异性推导要求**：
- 必须从故事梗概中**提取主角的核心优势或独特能力**，选择最能体现该优势的爽点模式
- 如果故事梗概中主角的核心爽感来源是智力、判断或布局 → 优先选择推理/布局类爽点，而非动作压制类
- 如果故事梗概中核心冲突是身份错位、秘密遮蔽或认知差 → 优先选择身份重估/真相揭露类爽点
- 如果已选要素强调成本、底线、仪式、潜台词或文艺母题 → 允许降低爽点密度，但要保证每章仍有微兑现
- **禁止**：仅根据题材标签选择爽点，必须结合故事梗概中的具体冲突机制

**爽点密度**（high=≥3/章，medium=1-2/章，low=≤1/章）：
- 如果故事梗概中冲突密集、事件频发 → high
- 如果故事梗概以渐进式积累和情感铺垫为主 → medium/low
- 如果故事梗概中存在大量内心戏和世界说明 → low

---

## 字段内容约束
- `hook_config` 写入偏好的钩子类型、强度基准和章尾是否强制留钩。
- `strand_config` 写入任务线、火线、星座线的最大缺席/连续阈值和停滞阈值，所有阈值为具体整数。
- `micro_payoff_config` 写入偏好的微兑现类型和每章最低数量。
- `cool_point_config` 写入偏好的爽点模式和每章密度，密度只使用 `high`、`medium`、`low`。
- 所有取值必须由故事梗概、已选叙事要素和上文推导表支持，不要输出分析字段。

## 质量校准
- 工作身份：你是一位项目特异性写作顾问，擅长从梗概、角色、世界规则和已选叙事要素中推导可执行的风格/结构契约。
- 推导路径：先识别本项目的核心卖点、冲突机制和表达风险，再把结论映射到字段级参数或编辑契约。
- 字段级质量示例（非输出格式）：平弱=“风格冷峻”；高质量=“说明冷峻来自何种场景压力、证据细节、对白比例或信息密度”。
- 可操作路径：每条规则都要能被正文生成、编辑检查或后续审计复用，避免泛化成所有项目都适用的空话。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否把项目特异性依据映射到具体字段/规则，而不是复用通用写作口诀。

{#- ---------------------------------------------------------------------------
    格式层：输出格式与 Schema
    --------------------------------------------------------------------------- -#}

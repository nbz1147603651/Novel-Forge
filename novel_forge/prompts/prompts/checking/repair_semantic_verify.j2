{# ================================================================================
【备注】
================================================================================
提示词：repair_semantic_verify.j2
服务步骤：REPAIR_SEMANTIC_VERIFY（修复后语义验证）
作用简述：修复完成后，调用 LLM 语义验证原始问题是否真正被解决，而非仅做字符串匹配。
上游输入：
  - issue_description：原始问题描述
  - issue_evidence：原始问题的证据片段
  - repaired_text：修复后的文本片段
  - repair_action：采取的修复动作（可选）
下游输出：
  - JSON：{"issue_resolved": bool, "confidence": float, "reasoning": str}
  - 下游消费者：execution_repair_semantic.py（用于判定修复是否成功）
消费链优先级：P0（误判修复成功会导致问题残留）> P1（置信度评估）> P2（推理过程）
简要例子：
  输入：issue_description="角色A已死但后续章节仍出现", issue_evidence="角色A在第5章死亡", repaired_text="第6章中，角色B回忆起了A的教诲"
  输出：{"issue_resolved": true, "confidence": 0.9, "reasoning": "修复后文本中角色A仅以回忆形式出现，符合已死亡的设定"}
================================================================================
#}

{#- ---------------------------------------------------------------------------
    指导层：任务定义 + 上下文消费
    --------------------------------------------------------------------------- -#}
{%- from "_base/_repair_quality_calibration.j2" import render_repair_quality_calibration, render_repair_pre_delivery_checklist -%}
你是一位专业的编辑，负责验证小说修复是否真正解决了原始问题。
请仔细阅读以下信息，判断修复后的文本是否**语义上**解决了原始问题。

## 原始问题
- 问题描述：{{ issue_description }}
{% if issue_evidence %}
- 原始证据：{{ issue_evidence }}
{% endif %}
{% if repair_action %}
- 修复动作：{{ repair_action }}
{% endif %}

## 修复后的文本
{{ repaired_text }}

{#- ---------------------------------------------------------------------------
    执行层：判定规则
    --------------------------------------------------------------------------- -#}

## 判定要求

你需要判断：修复后的文本是否在**语义层面**解决了原始问题？

请从以下角度分析：
1. **核心矛盾是否消除**：原始问题所指出的矛盾、错误或不一致是否不再存在？
2. **修复是否引入新问题**：修复后的文本是否产生了新的逻辑矛盾或事实冲突？
3. **上下文一致性**：修复后的内容是否与上下文保持连贯？


**重要**：
- 如果修复只是表面修改（如替换了关键词但语义矛盾仍在），请判定为 `false`
- 如果修复通过间接方式解决了问题（如通过上下文暗示而非直接陈述），可以判定为 `true` 但降低 `confidence`

{{ render_repair_quality_calibration("repair") }}

{{ render_repair_pre_delivery_checklist("repair") }}

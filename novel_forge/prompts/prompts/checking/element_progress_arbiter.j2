{# ================================================================================
【备注】
================================================================================
提示词：element_progress_arbiter.j2
服务步骤：ELEMENT_PROGRESS_ARBITER（要素灰区仲裁）
作用简述：对规则命中的灰区结果进行低频复判，输出统一的 hit/weak/miss 判定。
上游输入：
  - element：要素信息（element_id, name, category, prompt_hint, description）
  - rule_eval：规则判定结果（status, score, evidence）
  - context：上下文摘录（plan_excerpt, chapter_excerpt, quality_excerpt）
下游输出：
  - JSON：{"status", "confidence", "reason"}
  - 下游消费者：ElementProgressTracking 系统
消费链优先级：P2（风格建议类，仅用于灰区复核）
简要例子：
  输入：rule_eval={"status": "weak", "score": 1.2}, element={"name": "主角顿悟"}
  输出：{"status": "hit", "confidence": 0.78, "reason": "正文通过内心独白明确呈现了主角的感悟过程"}
================================================================================
#}

{#- ---------------------------------------------------------------------------
    指导层：任务定义 + 上下文消费
    --------------------------------------------------------------------------- -#}
请对"单个叙事要素在本章是否落实"进行复判。

## 任务说明
- 这是低频仲裁步骤，只用于规则灰区结果复核。
- 你必须在 `hit / weak / miss` 中三选一给出最终状态。
- `reason` 必须简短、可审计，聚焦证据而非泛泛评价。

## 要素信息
- element_id: {{ element.element_id }}
- name: {{ element.name }}
- category: {{ element.category }}
- prompt_hint: {{ element.prompt_hint }}
- description: {{ element.description }}

## 规则判定（供参考，不是最终结论）
- status: {{ rule_eval.status }}
- score: {{ rule_eval.score }}
- evidence: {{ rule_eval.evidence }}

## 上下文摘录
### 章节计划摘录
{{ context.plan_excerpt }}

### 正文摘录
{{ context.chapter_excerpt }}

### 质量反馈摘录
{{ context.quality_excerpt }}

## 质量校准
- 工作身份：你是一位严谨的小说质量审校员，擅长用短证据、明确边界和下游可执行建议定位问题。
- 审校路径：先确认输入证据，再判断问题类型、影响范围和修复边界，最后给下游可执行的定位信息。
- 字段级质量示例（非输出格式）：平弱=“这里不连贯”；高质量=“指出哪两处证据冲突、风险等级、可验证问题”。
- 可操作路径：每条判断都要能支持复核或修复，低置信度内容降级为待验证问题。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否给出短证据、风险等级、定位信息和下游可验证的问题。

{#- ---------------------------------------------------------------------------
    格式层：输出格式与 Schema
    --------------------------------------------------------------------------- -#}

`status` 只能是 `hit` / `weak` / `miss`；`confidence` 取值 0.0-1.0；`reason` 建议不超过 80 字。

{# ================================================================================
【备注】
================================================================================
提示词：plan_outline.j2
服务步骤：PLAN_OUTLINE（大纲规划）
作用简述：根据故事规格（Spec）和叙事要素选择（ElementSelection）生成完整
         叙事蓝图（Narrative Blueprint），为后续批量章节大纲生成提供结构约束。
上游输入：
  - spec: 用户输入的故事规格（genre, theme, tone, length_target, characters_hint,
          world_hint, conflict_hint, pov_hint, opening_style, ending_style 等）
  - story_bible: 来自 INIT_STORY_BIBLE（era, geography, culture, magic_or_tech,
                  rules, themes 等）
  - character_bible: 来自 INIT_CHARACTER_BIBLE（characters 列表及弧线信息）
  - blueprint_element_selection: 来自 BLUEPRINT_ELEMENT_SELECT（required_elements,
                                  extension_elements, focus_constraints）
  - style_profile: 来自 PROFILE_STYLE（summary, global_style, hook_config 等）
  - total_chapters: 用户指定的总章节数
  - use_volume_mode: 是否开启分卷模式
  - narrative_complexity: 叙事复杂度（simple/standard/complex/epic）
下游输出：
  - JSON（Narrative Blueprint）：synopsis, volume_mode, volumes,
    narrative_phases, key_turning_points, character_arcs, subplot_plan,
    suspense_schedule, ending_strategy
  - 下游消费者：
    * PLAN_OUTLINE_BATCH：消费 narrative_phases/key_turning_points 批量生成章节大纲
    * PLAN_CHAPTER：消费 synopsis/character_arcs/subplot_plan 生成章节级计划
    * DRAFT_CHAPTER：通过 narrative_contract 消费 world_rules/ending_strategy
消费链优先级：
  P0 = spec 核心字段（genre/tone/language）/ total_chapters / immutable rules
  P1 = story_bible 世界观约束 / character_arcs 映射 / key_turning_points
  P2 = style_profile 风格建议 / subplot_plan 交织结构
简要例子：
  输入：spec={genre:"玄幻", theme:"复仇", tone:"dark"}, total_chapters=100
  输出：{"synopsis":"...", "volume_mode":false, "volumes":[],
         "narrative_phases":[...], "key_turning_points":[...],
         "character_arcs":[...], "subplot_plan":[...],
         "suspense_schedule":[...], "ending_strategy":"..."}

【保护性注释 - 禁止删除以下内容】
- "世界与主题锚点"章节完整消费 story_bible 的 premise/era/geography/culture/magic_or_tech/rules/themes
  字段，这些是叙事蓝图的世界观基础约束，删除会导致后续章节大纲偏离世界观设定。
- "核心角色锚点"章节消费 character_bible.characters 的 name/role/personality/arc/relationships
  字段，这些是角色弧光计划的基础，删除会导致角色弧线缺失或与初始化设定矛盾。
- story_bible.rules 必须以"不可违背的世界规则"形式注入，PLAN_OUTLINE_BATCH 和 PLAN_CHAPTER
  都依赖这些规则生成符合世界观约束的章节计划。
================================================================================
#}

{#- ---------------------------------------------------------------------------
    指导层：任务定义 + 上下文消费
    --------------------------------------------------------------------------- -#}

{% set story_bible = story_bible | default({}) %}
{% set character_bible = character_bible | default({}) %}
{% set blueprint_element_selection = blueprint_element_selection | default({}) %}
{% set style_profile = style_profile | default({}) %}
{% set creative_director_packet = creative_director_packet | default({}) %}
{% set blueprint_fragment_request = blueprint_fragment_request | default(none) %}
{% set blueprint_generation_mode = blueprint_generation_mode | default("") %}
{% set blueprint_fragments_so_far = blueprint_fragments_so_far | default({}) %}
{% set fragment_key = blueprint_fragment_request.block_key if blueprint_fragment_request else "" %}
{% set is_blueprint_spine = blueprint_generation_mode == "quality_first_spine" or fragment_key == "spine" %}

{% if is_blueprint_spine %}
请根据以下故事规格，先生成一份全局叙事骨架。它不是最终细化稿，而是后续串行扩展的因果母版。
{% elif blueprint_fragment_request %}
请根据以下故事规格与已完成片段，生成叙事蓝图的指定片段。
{% else %}
请根据以下故事规格，生成完整的叙事蓝图（Narrative Blueprint）。
{% endif %}

## 故事规格
- 体裁：{{ spec.genre }}｜主题：{{ spec.theme }}｜基调：{{ spec.tone }}
- 目标字数：{{ spec.length_target }}｜语言：{{ describe_language(spec.language) }}
{% if spec.characters_hint | default('') %}- 角色：{{ spec.characters_hint }}{% endif %}
{% if spec.world_hint | default('') %}- 世界观：{{ spec.world_hint }}{% endif %}
{% if spec.conflict_hint | default('') %}- 核心冲突：{{ spec.conflict_hint }}{% endif %}
{% if spec.pov_hint | default('') %}- 叙事视角：{{ spec.pov_hint }}{% endif %}
{% if spec.opening_style | default('') %}- 开篇风格样张（仅供笔法参考，不得逐句复刻到第1章）：{{ spec.opening_style }}{% endif %}
{% if spec.ending_style | default('') %}- 收束风格样张（仅供笔法参考，不得逐句复刻到末章）：{{ spec.ending_style }}{% endif %}

{% if story_bible %}
## 世界与主题锚点
{% if story_bible.premise | default('') %}- 核心前提：{{ story_bible.premise }}{% endif %}
{% if story_bible.era | default('') %}- 时代背景：{{ story_bible.era }}{% endif %}
{% if story_bible.geography | default('') %}- 主要场域：{{ story_bible.geography }}{% endif %}
{% if story_bible.culture | default('') %}- 社会文化：{{ story_bible.culture }}{% endif %}
{% if story_bible.magic_or_tech | default('') %}- 异质系统：{{ story_bible.magic_or_tech }}{% endif %}
{% if story_bible.rules | default([]) %}
- 不可违背的世界规则：{{ story_bible.rules | join("；") }}
{% endif %}
{% if story_bible.themes | default([]) %}
- 主题线：{{ story_bible.themes | join("；") }}
{% endif %}
{% endif %}

{% if character_bible and character_bible.characters %}
## 核心角色锚点
以下角色弧线与人物关系应直接进入蓝图设计，不要只停留在类型标签：
{% for character in character_bible.characters %}
- {{ character.get('name', '') }}（{{ character.get('role', '') }}）{% if character.get('personality') | default('') %}：性格={{ character.get('personality') }}{% endif %}{% if character.get('arc') | default('') %}；弧线={{ character.get('arc') }}{% endif %}{% if character.get('relationships') | default({}) %}；关系={% for target, desc in character.get('relationships', {}).items() %}{{ target }}：{{ desc }}{% if not loop.last %} / {% endif %}{% endfor %}{% endif %}
{% endfor %}
{% endif %}

{% if relationship_overview | default([]) %}
## 初始化角色关系总览
以下关系来自最终 CharacterSystem，端点名必须保留，蓝图不得与其矛盾：
{% for rel in relationship_overview %}
- {{ rel }}
{% endfor %}
{% endif %}

{% set sel = blueprint_element_selection or {} %}
{% set required_elements = sel.required_elements if sel.required_elements is defined else [] %}
{% set extension_elements = sel.extension_elements if sel.extension_elements is defined else [] %}

## 叙事要素选择器注入
{% if sel.selector_summary is defined and sel.selector_summary %}- 选择结论：{{ sel.selector_summary }}{% endif %}
{% if sel.focus_constraints is defined and sel.focus_constraints %}
- 执行约束：{{ sel.focus_constraints | join("；") }}
{% endif %}
{% if required_elements %}
- 必要项（必须全部体现）：
{% for item in required_elements %}
  - {{ item.name }}（{{ item.element_id }}）：{{ item.description }}
    提示词：{{ item.prompt_hint }}
{% endfor %}
{% endif %}
{% if extension_elements %}
- 扩展项（按本作机制优先级）：
{% for item in extension_elements %}
  - {{ item.name }}（{{ item.element_id }} / {{ item.category }}）
    {% if item.selection_score is defined and item.selection_score %}优先级：{{ item.selection_score }}{% if item.selection_source is defined and item.selection_source %}｜来源：{{ item.selection_source }}{% endif %}
    {% endif %}
    适配题材：{{ item.recommended_genres | default([]) | join("、") }}
    选用原因：{{ item.selection_reason | default("") }}
    提示词：{{ item.prompt_hint }}
{% endfor %}
{% endif %}

{% if style_profile %}
## 项目风格规范注入
{% if style_profile.summary | default('') %}- 风格概述：{{ style_profile.summary }}{% endif %}
{% if style_profile.global_style | default({}) %}
- 对话占比：{{ style_profile.global_style.dialogue_ratio | default("") }}
- 节奏模式：{{ style_profile.global_style.pace_mode | default("") }}
- 情绪风格：{{ style_profile.global_style.emotional_style | default("") }}
{% endif %}
{% if style_profile.hook_config | default({}) %}
- 钩子偏好：{{ style_profile.hook_config.preferred_types | default([]) | join("、") }}
- 强度基准：{{ style_profile.hook_config.strength_baseline | default("") }}
{% endif %}
{% if style_profile.cool_point_config | default({}) and style_profile.cool_point_config.preferred_patterns | default([]) %}
- 爽点模式：{{ style_profile.cool_point_config.preferred_patterns | join("、") }}
{% endif %}
{% if style_profile.strand_config | default({}) %}
- 三线节奏：Quest≤{{ style_profile.strand_config.quest_max_consecutive | default("") }}章,
            Fire≤{{ style_profile.strand_config.fire_max_absent | default("") }}章断档,
            Constellation≤{{ style_profile.strand_config.constellation_max_absent | default("") }}章断档
{% endif %}
{% endif %}

{% if creative_director_packet %}
## 创作导演包（用于增强创作性，不改变 JSON 字段结构）
请优先把以下内容转化为情节选择、场景张力和支线交织，而不是生硬复述：
{% if creative_director_packet.emotional_engine | default([]) %}- 情感引擎：{{ creative_director_packet.emotional_engine | join("；") }}{% endif %}
{% if creative_director_packet.thematic_promises | default([]) %}- 主题承诺：{{ creative_director_packet.thematic_promises | join("；") }}{% endif %}
{% if creative_director_packet.relationship_tensions | default([]) %}- 关系张力：{{ creative_director_packet.relationship_tensions | join("；") }}{% endif %}
{% if creative_director_packet.anti_cliche_rules | default([]) %}- 反套路约束：{{ creative_director_packet.anti_cliche_rules | join("；") }}{% endif %}
{% if creative_director_packet.scene_potential | default([]) %}- 关键场面潜力：{{ creative_director_packet.scene_potential | join("；") }}{% endif %}
{% endif %}

{% if blueprint_fragments_so_far %}
## 已完成蓝图片段
这些片段已经通过本地缓存/校验，本次内容必须与其保持一致，不要重复处理未要求字段：
{% if blueprint_fragments_so_far.snapshot_mode is defined %}
当前提供的是压缩后的因果锚点快照；被省略的细节不代表可以改写。请优先继承 anchors 中的主线转折、支线触发/反哺/收束、角色变化和悬念兑现关系。
{% endif %}
```json
{{ blueprint_fragments_so_far | tojson }}
```
{% endif %}

{% if blueprint_fragment_request %}
## 本次片段任务
- 片段：{{ blueprint_fragment_request.title }}（{{ fragment_key }}）
- 只允许输出字段：{{ blueprint_fragment_request.required_keys | join("、") }}
{% if is_blueprint_spine %}
- 本次是 quality_first 模式的整体骨架：允许输出全部蓝图顶层字段，但每个字段只写决定后续因果的必要信息。
- 先确定“主线转折 → 角色变化 → 支线触发/反哺/收束 → 终局回收”的统一链条；后续片段会在此基础上扩写。
- subplot_plan 必须先给出每条支线的主线锚点、反哺节点和收束目标，避免支线变成独立平行线。
{% elif blueprint_generation_mode == "quality_first_expansion" %}
- 本次是 quality_first 模式的串行扩展：只能输出本片段字段，但必须继承已完成骨架和前序片段的因果锚点。
- 不要改写已确定的主线转折、支线反哺或收束目标；如需细化，请把细节写入本片段允许字段。
{% else %}
- 本次是蓝图片段化生成：只阅读并执行与该片段相关的要求；未点名字段不得输出。
{% endif %}
{% if blueprint_fragment_request.instructions %}
{% for item in blueprint_fragment_request.instructions %}
- {{ item }}
{% endfor %}
{% endif %}
{% endif %}

## 蓝图要求
生成可用于“分批章节大纲生成”的叙事蓝图（Narrative Blueprint）。本项目为长篇结构化写作，请使用 **章节编号区间**（不是百分比）来划分阶段与转折点。

已知规划：
- 总章节数：{{ total_chapters }}
- 分卷模式：{{ "开启" if use_volume_mode else "关闭" }}

### 全局章节引用约束
- 所有结构化章节字段必须使用真实章节号，且不得越过 1..{{ total_chapters }}。
- 任何文本字段中若显式写出“第N章”，N 必须落在该字段所属结构的章节区间内：分卷文本必须落在该卷 start_chapter/end_chapter，叙事阶段文本必须落在该阶段 chapter_start/chapter_end，角色弧光里程碑文本必须落在该 milestone 区间。
- 若只想表达相对节奏，请写“本卷前段/本阶段后段/中段反转”等相对位置，不要硬写超出所属范围的章节号。
- 结构化字段必须写成 JSON key；禁止把 `.chapter_start: 19`、`.chapter_end: 36`、`field=value` 这类字段尾注塞进 `description` 或其他文本字段。

{% if not blueprint_fragment_request or fragment_key == "overview" or is_blueprint_spine %}
### 1. 全书概述 (synopsis) ⚠️ 必填字段
- **此字段为必需字段，不可省略、不可为空字符串**
- 用2-4句话概括故事主线、核心冲突与结局方向
- 语言风格与基调符合：{{ spec.tone }}（体裁：{{ spec.genre }}）
- 输出 JSON 时必须包含 `synopsis` 字段，值为非空字符串。

### 2. 分卷规划（volume_mode / volumes）
{% if use_volume_mode %}
分卷模式已开启，请为每个卷填写以下 **全部字段**，不得留空：

- **volume_number**：卷序号（1 开始，连续递增）
- **title**：卷标题，体现本卷核心主题；必须来自本作意象、冲突或阶段功能，不套用外部题材词或模板化雅词。
- **start_chapter** / **end_chapter**：章节区间（连续不重叠，覆盖 1..{{ total_chapters }}）
- **arc_goal**：本卷需要完成的叙事目标（一句话，写清本卷让哪条主线、关系或身份发生何种可验证变化）
- **milestone_targets**：本卷必须达成的里程碑列表（2-4 条，每条为本作输入可推导的具体事件；若写“第N章”，N 必须落在本卷区间内）
- **main_conflicts**：本卷核心冲突列表（1-3 条，直接来自本作主冲突、人物目标、世界规则或关系压力）
- **climax_hint**：本卷高潮方向提示（描述卷末高潮场景的核心事件与冲突升级方式）
- **resolution_hint**：本卷收束方式提示（描述卷末状态：谁获得了什么/失去了什么，什么悬念被解答，什么悬念被留下，下一卷的入口是什么）

⚠️ 以上字段全部必填，不得使用空数组 [] 或空字符串 ""。
{% else %}
分卷模式已关闭：volume_mode=false 且 volumes=[]
{% endif %}
{% endif %}

{% if not blueprint_fragment_request or fragment_key == "phases" or is_blueprint_spine %}
### 3. 叙事阶段 (narrative_phases)
- 3-6 个阶段
- 每个阶段必须覆盖一个 **连续章节区间**：chapter_start / chapter_end
- 同时给出该阶段的关键事件、时间锚点、主要场景与核心出场人物，用于降低漂移
{% endif %}

{% if not blueprint_fragment_request or is_blueprint_spine %}
### 3.1 每章节奏曲线 (chapter_rhythm_curve)
- 为 1..{{ total_chapters }} 每章各输出一个对象，不得跳章、不得重复章节号。
- 每个对象只包含：chapter_number、target_pacing、target_tension、beat_pattern、description。
- target_pacing 为 1-5 整数：1=舒缓沉潜，2=偏慢铺垫，3=中速推进，4=偏快加压，5=急促高压。
- target_tension 为 1-5 整数：1=低张力余波，2=轻压，3=中等压力，4=高压冲突，5=峰值/临界。
- beat_pattern 用一个短字符串描述章内节拍，如“舒缓-窒息-释放”“钩子-追问-悬停”；不要写数组。
- description 用一句话说明本章节奏安排如何服务主线、人物或悬念，不写泛泛气氛词。
{% endif %}

{% if not blueprint_fragment_request or fragment_key == "turning_points" or is_blueprint_spine %}
### 4. 关键转折 (key_turning_points)
- 列出 3-8 个关键转折点，每个转折必须绑定到一个具体 chapter_number
- 对每个转折给出发生场景 location 与参与角色 characters_involved
- key_turning_points 同时是可视化界面的“主线骨架”：这些节点必须串成从开端到终局的因果链，description 要写清主线冲突在此处发生的状态变化，不要只写氛围、支线事件或角色日常
- 建议包含开局触发点、至少一个中段反转/升级点、最终收束点；若总章数已知，最终收束点应靠近最后一章
{% endif %}

{% if not blueprint_fragment_request or fragment_key == "character_arcs" or is_blueprint_spine %}
### 5. 角色弧光 (character_arcs) 与情感弧线 (emotional_arcs)
- 2-6 条核心角色弧光
- **概念边界：角色弧光聚焦"内在变化"，而非事件线**
  - 内在变化包括：认知转变、情感起伏、价值观重塑、信念坚守/崩塌
  - 弧光描述的是角色**内心**的成长或堕落，不包含独立的事件进程
  - 若一条线索有自己的人物、事件、起承转合、收束，那是**支线**，不是角色弧光
- milestones 需按章节区间给出多个里程碑（chapter_start/chapter_end），并描述该里程碑的具体变化/选择/代价
- `character_arcs[].milestones[]` 每个对象只允许 3 个字段：`chapter_start`、`chapter_end`、`description`
- milestone 的补充说明、心理变化、选择代价和阶段功能全部合并进 `description` 的一句中文描述；字段名只使用本节列出的英文 `snake_case` key
- `description` 必须是纯文本字符串，禁止写成对象或带子字段的结构

#### 5.1 情感弧线 (emotional_arcs)
- 本片段**必须同时**输出 `character_arcs` 与 `emotional_arcs` 两个顶层字段，缺一会被契约拒绝。
- 2-4 条情感弧线，跟踪整本书的情绪起伏曲线。
- 每条情感弧线字段：arc_name（中文名）、emotion_type（紧张/温馨/悬疑/悲壮/欢快等）、peak_chapters（情感高峰章节号数组）、valley_chapters（情感低谷章节号数组）、description（1-2 句中文描述）、related_characters（相关角色名数组）、related_subplots（相关支线名数组）。
- peak_chapters 与 valley_chapters 应覆盖全书范围并与 character_arcs 的里程碑章节对齐。
- 写法上先写完 character_arcs，再写 emotional_arcs，不要把后者合并到前者的 description 里。
{% endif %}

{% if not blueprint_fragment_request or fragment_key == "subplots" or is_blueprint_spine %}
### 6. 支线规划 (subplot_plan)
- **概念边界：支线是"独立叙事线"，有自己的人物、事件、起承转合、收束**
  - 支线区别于角色弧光：支线有外部事件进程，角色弧光是内在心理变化
  - 支线区别于主线：支线服务于主线主题或角色，但有独立的故事弧
- **复杂度引导**（按叙事复杂度推荐支线数量）：
  {%- if narrative_complexity == "simple" %}
  根据叙事复杂度（简单），建议规划 **1-2 条支线**。
  {%- elif narrative_complexity == "standard" %}
  根据叙事复杂度（标准），建议规划 **2-4 条支线**。
  {%- elif narrative_complexity == "complex" %}
  根据叙事复杂度（复杂），建议规划 **3-6 条支线**。
  {%- elif narrative_complexity == "epic" %}
  根据叙事复杂度（史诗），建议规划 **4-8 条支线**。
  {%- else %}
  根据叙事复杂度，建议规划 **2-4 条支线**（默认标准）。
  {%- endif %}
  - **支线来源推导**：
  根据本作品的体裁({{ spec.genre }})、核心冲突({{ spec.conflict_hint }})、
  角色阵容、世界规则与已选叙事要素，自主推导 2-6 条支线。推导方向只作为功能轴参考，不是题材模板：
  - 情感/关系线（角色间的张力与变化）
  - 谜团/探索线（信息不对称与揭示节奏）
  - 势力/博弈线（派系冲突与权力动态）
  - 成长/能力线（角色内在或外在能力的进阶）
  - 世界观/规则线（设定揭示与验证）
 - **密度约束**：每条支线至少 3-5 个 chapter_events（确保支线有足够叙事容量）
 - **交织结构约束**（每条支线必须同时满足以下 3 项）：
   ① 至少 1 条【主线→支线】link（link_type 必须为 trigger_start 或 trigger_turn）
   ② 至少 1 条【支线→主线】link（link_type 必须为 feed_main 或 reveal_key）
   ③ theme_echo / create_tension / constrain 为加分项，**不计入**上述最低要求
  - 每条支线给出 involved_chapters（章节编号列表），避免后续大纲无法落地
  - **交叉约束**：`weave_links` 中每个 `trigger_chapter` 必须包含在 `involved_chapters` 中。若支线在某章反哺主线，该章必须同时出现在 `involved_chapters` 和对应 weave_link 的 `trigger_chapter`
  - 每条支线必须给出 chapter_events（逐章节节点事件），每个节点绑定 chapter_number 与 event，章节号需与 involved_chapters 对齐

#### 支线交织关系（核心原则：环环相扣、最终收敛、服务于主线）
 - **全局交织密度**：
   - 全书层面：支线→主线的反哺关系（feed_main/reveal_key）总数 ≥ 支线数量
   - 时间分布：交织触发章节应分散在不同叙事阶段，避免集中在单一阶段
   - 主线转折关联：至少半数已规划关键转折应有支线 weave_link 指向
 - **每条支线必须与主线形成闭环**：
   - 主线触发支线启动（至少一个 link_type='trigger_start'，禁止仅用 trigger_turn 替代）
   - 支线反哺主线推进（至少一个 link_type='feed_main' 或 'reveal_key'，**禁止使用 theme_echo 替代**）
 - **支线间可形成因果链**：A 的结果触发 B 的启动，但最终都需收敛到主线
 - **交织关系类型**（link_type）：
   - 【主线→支线】trigger_start（触发启动）/ trigger_turn（触发转折）/ constrain（约束走向）/ enable（提供条件）/ conflict（制造冲突）
   - 【支线→主线】feed_main（反哺主线转折）/ reveal_key（揭露关键信息）/ create_tension（制造主线张力）/ theme_echo（呼应主题升华）
 - **收束路径规划**（每条支线必须明确）：
   - resolution_chapter: 在哪一章收束
   - resolution_target: 收束到哪里（**primary/normal 优先级必须为** 'main_turning_point:N' 或 'character_fate:角色名'；仅 background 优先级允许 'theme_echo'）
   - resolution_type: 如何收束（'resolve'问题解决 / 'reveal'悬念揭示 / 'ascend'价值升华 / 'merge'并入主线）
 - 对于 chapter_events 中的关键节点，若涉及与其他线索的互动，填入 weave_notes 说明
 - 若某节点依赖前序支线结果，`depends_on` 必须填写为数组；单个依赖也写成 ["支线名:章节号"]，没有依赖写 []，禁止写成字符串
 - `weave_links` 必须是对象数组；数组元素只能是 `{...}` 对象，禁止插入空字符串 `""`，禁止把 `"source_type": ...` 这类字段直接摊平在数组里
 - 为每条支线标注 priority（primary/normal/background）
{% endif %}

{% if not blueprint_fragment_request or fragment_key == "ending" or is_blueprint_spine %}
### 7. 收束策略 (ending_strategy)
- 说明如何回收主线悬念与伏笔、角色命运安排、主题呼应方式
- `ending_strategy` 必须是一个字符串段落，禁止写成对象、数组或任何嵌套结构；如需包含人物结局/悬念回收/终局画面，请全部压缩进这个字符串。
{% endif %}

{% if not blueprint_fragment_request or fragment_key == "suspense" or is_blueprint_spine %}
### 8. 悬念时间表 (suspense_schedule)

为全书规划核心悬念节点，确保悬念铺设与兑现节奏可控：

#### 悬念类型（必须从以下5种中选择）：
- **mystery（谜团）**：读者与角色尚未获得的关键信息、真相或规则缺口。
- **crisis（危机）**：迫近的外部威胁、损失风险或局势失控点。
- **emotion（情感）**：关系变化、情绪冲击、信任裂缝或亲密张力。
- **choice（抉择）**：会改变代价、阵营、关系或主线方向的选择。
- **desire（渴望）**：角色强烈追求被阻断、延迟或重新定义。

#### 悬念紧迫度（决定悬念持续张力）：
- **critical（临界）**：悬念必须在当前章节立即解决
- **high（高）**：悬念应在2-3章内兑现
- **normal（正常）**：悬念可跨越5-10章后兑现
- **low（低）**：悬念可作为全书伏笔，长线铺垫

#### 规划要求：
 - 每条悬念必须关联 **related_subplot**（支线名称），形成悬念-支线绑定
 - `related_subplot` 只能写 `"主线"` 或已经存在的支线名称完整原文；禁止临时发明任何未在支线规划中建档的名字
 - 若悬念属于角色弧光、主线反转或人物抉择，但没有对应独立支线，则 related_subplot="主线"
 - 若确实需要独立支线，必须先在支线规划中建立同名支线，再在 `suspense_schedule` 精确引用该名称
 - 每条悬念需规划 introduce_chapter（引入章节）与 resolve_chapter（计划兑现章节）
 - 悬念总数建议 3-8 条，覆盖全书关键节点
 - **strand_affinity**（追读力系统必需）：每条悬念需标注与三条情节线的关联度
   - quest（主线推进）/ fire（情感爆发）/ constellation（伏笔编织）
   - 值为 0-1 的浮点数，总和约等于 1.0；根据该悬念实际推动的功能估算，不要机械平均。
{% endif %}

{% if not blueprint_fragment_request or is_blueprint_spine %}
### 9. 追读力边界
- 本任务只规划全书级 `suspense_schedule`，不得输出 `chapter_hooks`。
- 每章 `expected_hook` 与 `expected_payoffs` 由后续 PLAN_OUTLINE_BATCH / PLAN_OUTLINE_CONTINUE 负责生成，当前蓝图不得逐章展开。
{% endif %}

## 项目特异性引导
- 不按题材标签套固定桥段。先从 `spec`、`story_bible`、`character_bible`、已选叙事要素中提取本作真正的冲突机制、信息差、关系压力、世界规则和兑现承诺。
- 如果某类题材惯例与本作输入冲突，以本作输入为准；若输入没有支持，不要为了“像某题材”而新增谜团、能力体系、历史事件、恋爱障碍或其他外部模板。
- 所有特色设计都要落回可验证的章节节点、角色变化、支线反哺或悬念兑现，不写泛泛的类型口号。

{% include "_shared/_outline_quality_invariants.j2" %}

{#- ---------------------------------------------------------------------------
    系统契约由 PromptBuilder 注入
    --------------------------------------------------------------------------- -#}

{% if blueprint_fragment_request %}
{% set fragment_key = blueprint_fragment_request.block_key %}
## 本次片段字段要求
{% if is_blueprint_spine %}
输出全部蓝图顶层字段：`synopsis`、`volume_mode`、`volumes`、`narrative_phases`、`key_turning_points`、`character_arcs`、`subplot_plan`、`suspense_schedule`、`ending_strategy`。这是全局骨架稿：每个数组控制在可读的核心节点内，但必须保留主线、支线、人物弧、悬念和结局之间的因果锚点。
{% elif fragment_key == "overview" %}
输出 `synopsis`、`volume_mode`、`volumes`。`synopsis` 用 2-4 句话概括故事主线、核心冲突、情感/主题承诺与终局方向；`volume_mode` 与当前卷模式一致；`volumes` 在非卷模式下为空数组。
{% elif fragment_key == "phases" %}
输出 `narrative_phases`。每个阶段写阶段名、起止章节、阶段描述、关键事件、张力等级、时间语境、主要地点和关键角色；章节区间必须覆盖全书且不重叠。
{% elif fragment_key == "turning_points" %}
输出 `key_turning_points`。每个转折点写章节号、转折描述、发生地点和涉及角色；转折必须能改变主线方向、关系状态或核心悬念。
{% elif fragment_key == "character_arcs" %}
输出 `character_arcs` 与 `emotional_arcs` 两个顶层字段。先完整写完并闭合 `character_arcs` 数组，再写同级 `emotional_arcs` 数组；禁止把 `emotional_arcs` 放进 `character_arcs[]` 内部。每个角色弧写角色名、弧光概述和 milestones；每个 milestone 只包含 `chapter_start`、`chapter_end`、`description`。
{% elif fragment_key == "subplots" %}
输出 `subplot_plan`。每条支线写名称、描述、涉及章节、章节事件、交织链接、优先级、收束章节、收束目标和收束类型。`chapter_events` 每项写章节号、事件、交织说明和依赖数组；`weave_links` 每项写来源类型、来源引用、目标支线、触发章节、链接类型和描述。
{% elif fragment_key == "suspense" %}
输出 `suspense_schedule`。每条悬念写稳定 ID、悬念类型、引入章节、计划兑现章节、描述、紧迫度、关联支线和 `strand_affinity`；`related_subplot` 只能写 `"主线"` 或已完成支线名称的完整原文，不得新增支线名；`strand_affinity` 固定包含 quest、fire、constellation 三个 0-1 浮点值。
{% elif fragment_key == "ending" %}
输出 `ending_strategy` 字符串，说明主线、人物弧光、主题承诺和关键悬念如何在结尾落地；不要新增字段，也不要把 `ending_strategy` 的值写成对象。
{% endif %}
{% else %}
## 全量蓝图字段要求
- `synopsis`：2-4 句话概括故事主线、核心冲突、情感/主题承诺与终局方向。
- `volume_mode` 与当前卷模式一致；`volumes` 在卷模式下写卷号、标题、起止章节、卷目标、里程碑、主冲突、高潮方向和收束方式，非卷模式下为空数组。
- `narrative_phases` 写完整阶段数组，每个阶段包含阶段名、起止章节、描述、关键事件、张力等级、时间语境、主要地点和关键角色。
- `key_turning_points` 写全书关键转折，每项包含章节号、描述、地点和涉及角色。
- `character_arcs` 写角色弧数组，每项包含角色名、弧光概述和 milestones；milestones 每项只包含 `chapter_start`、`chapter_end`、`description`。
- `subplot_plan` 写支线计划，每条支线包含名称、描述、涉及章节、章节事件、交织链接、优先级、收束章节、收束目标和收束类型。
- `suspense_schedule` 写 3-8 条核心悬念，每条包含稳定 ID、类型、引入章节、兑现章节、描述、紧迫度、关联支线和 `strand_affinity`；`related_subplot` 只能写 `"主线"` 或 `subplot_plan[].name` 的完整原文。
- `ending_strategy` 说明主线、人物弧光、主题承诺和关键悬念如何在结尾落地。
- `chapter_rhythm_curve` 写 1..{{ total_chapters }} 每章的节奏目标，每项包含 chapter_number、target_pacing(1-5)、target_tension(1-5)、beat_pattern(字符串)、description。
{% endif %}

{% if blueprint_fragment_request %}
## 重要约束
1. 本次蓝图片段需覆盖：`{{ blueprint_fragment_request.required_keys | join("`, `") }}`
2. 内容必须与“已完成蓝图片段”保持因果、角色、章节编号一致
3. 所有章节编号必须在 1..{{ total_chapters }} 内
{% if is_blueprint_spine %}
- **骨架约束**：每条支线必须写明主线触发、支线反哺和最终收束三类功能；后续扩展不得推翻这些因果锚点。
{% elif fragment_key == "subplots" %}
- **支线编织约束**：`weave_links` 必须是对象数组；禁止空字符串占位，禁止写成 `["", "source_type": ...]` 或 `[{}, "", "source_type": ...]`
{% elif fragment_key == "character_arcs" %}
- **角色弧字段约束**：`character_arcs[].milestones[]` 每个对象只允许 `chapter_start`、`chapter_end`、`description` 三个字段；补充说明全部并入 `description`
{% elif fragment_key == "ending" %}
- **结局字段约束**：`ending_strategy` 的值必须是字符串；禁止对象、数组、嵌套字段和额外字段
{% endif %}
{% else %}
## 重要约束
1. 确保各部分相互协调，形成统一的故事愿景
2. 主线、支线、人物弧、悬念和结局必须互相校准，支线不得像独立轨道后贴上去。
3. narrative_phases 的章节区间必须覆盖 1..{{ total_chapters }}，且区间不重叠、不断档
4. key_turning_points 与 character_arcs 的 milestones 必须落在有效章节编号内
5. 保持与故事规格的一致性，禁止脱离体裁/基调/语言要求
6. 蓝图必须可操作，能直接指导后续分批章节大纲生成
7. `subplot_plan[].chapter_events` 中每个 `chapter_number` 必须属于对应 `involved_chapters`
8. **支线密度约束**：`subplot_plan` 中每条支线的 `chapter_events` 数量 ≥ 3
9. **支线交织结构约束**：
   a. 每条支线 `weave_links` ≥ 2
   b. 其中至少 1 条 `link_type` 为 `trigger_start` 或 `trigger_turn`（主线触发支线）
   c. 其中至少 1 条 `link_type` 为 `feed_main` 或 `reveal_key`（支线反哺主线）
   d. 禁止所有 `weave_links` 都是 `theme_echo`/`create_tension`/`constrain`
10. **悬念 strand_affinity**：`suspense_schedule` 中每条悬念必须包含 `strand_affinity` 字段，key 为 quest/fire/constellation，值为 0-1 浮点数
11. `subplot_plan[].chapter_events[].depends_on` 必须始终是 JSON 数组；单个依赖写成 ["支线名:章节号"]，没有依赖写 []
12. `character_arcs[].milestones[]` 每个对象只允许 `chapter_start`、`chapter_end`、`description` 三个英文 `snake_case` 字段；补充说明必须写进 description
13. `suspense_schedule[].related_subplot` 必须精确匹配 `"主线"` 或某条 `subplot_plan[].name`；如果是角色弧光节点且没有独立支线，写 `"主线"`，不要新造支线名
14. `chapter_rhythm_curve` 必须覆盖 1..{{ total_chapters }}，target_pacing 与 target_tension 必须是 1-5 整数，beat_pattern 必须是字符串。
{% endif %}

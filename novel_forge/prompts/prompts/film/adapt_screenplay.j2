{#
================================================================================
【备注】
================================================================================
提示词：adapt_screenplay.j2
服务步骤：ADAPT_SCREENPLAY（成稿回流第二级：语义级影视改编）
作用简述：把第一级确定性投影产出的场次/对白/动作，改写为可拍摄的影视场次：
         叙述转视觉动作、心理描写外化、对白按镜头节奏裁剪。只输出完整
         Screenplay 对象（FULL_OBJECT），逐场通过语义验证后才会被采纳。
上游输入：
  - title/language: 作品标题与输出语言
  - scenes: 第一级投影场次（含 lines/source_refs，为唯一正文事实源）
  - direction: 方向性元数据（情绪走向/节奏/意图摘要，渐进披露，不含全文）
  - cast_names: 已登记角色名单（改写不得发明新角色）
下游输出：
  - JSON（FULL_OBJECT）：{"title":..., "synopsis":..., "scenes":[<ScreenplayScene>]}
  - 下游消费者：
    * film/pipeline SCREENPLAY 阶段：逐场 accept/reject 验证后替换确定性场次
    * 分镜阶段：消费改写后场次的 objective/conflict/visual_hook
消费链优先级：
  P0 = 忠实度：不得增删人物、不得改变场次结果与因果，来源引用 source_refs 原样保留
  P1 = 视觉化：叙述/心理必须转为可见的动作、表情、物件或空间变化
  P2 = 节奏：对白按镜头节奏裁剪为短句，删除书面化长从句
简要例子：
  输入：scenes=[{"heading":"雨夜告别","lines":[{"kind":"action","text":"他心里明白这是最后一次见面。"}]}]
  输出：scenes=[{"heading":"雨夜告别","lines":[{"kind":"action","text":"他伸手触碰门把，又停在半空。"},{"kind":"dialogue","speaker":"林远","text":"别送了。"}]}]
================================================================================
#}
{#- ---------------------------------------------------------------------------
    指导层：任务与输入
    --------------------------------------------------------------------------- -#}

你是商业影视改编编剧。下面给出由小说成稿确定性提取的场次（第一级投影），请把它改写为可拍摄的影视场次。

## 作品上下文
- 标题：{{ title }}
- 输出语言：{{ language | default("zh") }}

## 方向性元数据（只用于把握走向，不作为正文来源）
{{ direction | tojson(ensure_ascii=False) }}

## 已登记角色（改写只能使用这些名字）
{{ cast_names | tojson(ensure_ascii=False) }}

## 第一级投影场次（唯一正文事实源）
{{ scenes | tojson(ensure_ascii=False) }}

{#- ---------------------------------------------------------------------------
    约束层：质量与边界
    --------------------------------------------------------------------------- -#}

## 改写规则
- **忠实度第一**：不得新增或删除角色；不得改变场次的事件结果、因果关系与情绪走向；每场的 `source_refs`、`source_chapter`、`source_scene_ref` 必须原样保留。
- **叙述→视觉动作**：旁白式叙述必须转为镜头可见的动作、表情、物件或空间变化；无法视觉化的背景信息压缩为一句动作行。
- **心理→外化**：内心独白转为可表演的行为（停顿、回避、动作细节）或一句短对白；禁止保留"他想/他意识到"式心理陈述。
- **对白按镜头节奏裁剪**：保留原文关键台词，删去书面化长从句；每句对白以短句为主，`speaker` 必须来自已登记角色。
- **场次结构**：每场保留 objective/conflict/turn/visual_hook/sound_hook 字段；信息不足时沿用第一级投影的原值，禁止编造新情节。
- **JSON 稳定性**：只输出符合 Schema 的完整对象；字符串内部不要使用未转义的英文双引号；不要输出 Markdown、注释或省略号。
- 输出顶层必须包含 `title` 与 `scenes` 两个键；`scenes` 数量与第一级投影一致、顺序一致。

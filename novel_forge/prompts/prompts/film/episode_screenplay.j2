{#
================================================================================
【备注】
================================================================================
提示词：episode_screenplay.j2
服务步骤：EPISODE_SCREENPLAY（短剧全流程：单集剧本）
作用简述：把一集大纲展开为可拍摄短剧剧本：3-5 场、全集 12 镜、18-25 句对白
         的硬标准；对白短句化、动作可视化。只输出完整对象（FULL_OBJECT）。
上游输入：
  - outline: 本集大纲（场次摘要/卡点标记，唯一结构事实源）
  - series_plan_meta: 方向性元数据（题材/波形阶段/卡点策略，渐进披露）
  - cast_names: 已登记角色名单（对白 speaker 只能取这些名字）
下游输出：
  - JSON（FULL_OBJECT）：{"episode_number":..., "scenes":[<DramaScene>]}
  - 下游消费者：
    * 制作包导出：场次/对白/镜数登记入集包
    * 分镜/拍摄：shot_count 分配镜头配额
消费链优先级：
  P0 = 硬标准：3-5 场、全集 shot_count 合计 12、对白 18-25 句，验证不过直接退回
  P1 = 卡点落位：paywall_marker 指示的卡点必须落在对应位置（start/middle/end）
  P2 = 短剧节奏：单句对白 ≤20 字优先，冲突前置，禁止长段旁白
简要例子：
  输入：outline 场次 3，paywall_marker="identity_reveal@end"
  输出：scenes=[{"scene_number":1,"heading":"宴会厅·夜","action":"灯光骤暗。","shot_count":4,"lines":[{"speaker":"林晚","line":"你敢再说一遍？"}]}]（末场结尾落在身份揭晓瞬间）
================================================================================
#}
{#- ---------------------------------------------------------------------------
    指导层：任务与输入
    --------------------------------------------------------------------------- -#}

你是商业短剧执行编剧。请把下面这集大纲展开为可拍摄剧本，严格遵守硬标准。

## 本集大纲（唯一结构事实源）
{{ outline | tojson(ensure_ascii=False) }}

## 方向性元数据（只用于把握走向，不作为正文来源）
{{ series_plan_meta | tojson(ensure_ascii=False) }}

## 已登记角色（对白 speaker 只能使用这些名字）
{{ cast_names | tojson(ensure_ascii=False) }}

{#- ---------------------------------------------------------------------------
    约束层：质量与边界
    --------------------------------------------------------------------------- -#}

## 剧本规则
- **硬标准**：`scenes` 恰为大纲场次数（3-5 场）；各场 `shot_count` 合计必须等于 12；全集对白（`lines` 中 `line` 非空项）合计必须在 18-25 句之间。
- **卡点落位**：大纲 `paywall_marker` 形如 `<strategy>@<position>`：`start` 落首场开头、`middle` 落中点场结尾、`end` 落末场最后一句之前收束；卡点句必须制造即时悬念。
- **对白规范**：`speaker` 只能取已登记角色名；单句以短句为主（20 字内优先）；禁止书面化长从句与内心独白陈述。
- **动作可视化**：每场 `action` 写镜头可见的动作与空间变化；禁止"他心想"式心理陈述。
- **编号连续**：`scene_number` 从 1 连续；`heading` 写清场景与日夜。
- **JSON 稳定性**：只输出符合 Schema 的完整对象；`shot_count` 必须是整数；不要输出 Markdown、注释或省略号。
- 输出顶层必须包含 `episode_number` 与 `scenes` 两个键。

{%- from "_base/_summary_pass.j2" import render_summary_pass -%}
{#
================================================================================
【备注】
================================================================================
提示词：summarize_chapter.j2
服务步骤：SUMMARIZE_CHAPTER（章节摘要）
作用简述：将章节正文压缩为高密度摘要，供后续创作检索和上下文注入使用
上游输入：
  - chapter_number/chapter_title: 章节信息
  - outline.goal: 章节目标（可选）
  - target_words: 目标字数（默认 200）
  - chapter_text/text: 章节正文
下游输出：
  - JSON：summary, key_events, character_state_changes, unresolved_questions, new_facts
  - 下游消费者：Memory 系统 / VolumeAudit / 后续章节 Bridge
消费链优先级：
  P0 = summary 必填 / 时间顺序可追溯
  P1 = key_events 覆盖主事件 / character_state_changes 准确性
  P2 = unresolved_questions 悬念保留
简要例子：
  输入：chapter_text="林远站在城门口...", chapter_number=3
  输出：{"summary":"...","key_events":["林远抵达城门"],"character_state_changes":[...],...}
================================================================================
#}

{#- ---------------------------------------------------------------------------
    指导层：任务定义与输入消费
    --------------------------------------------------------------------------- -#}

请将以下章节内容压缩为可供后续创作检索的高密度摘要。

{{ render_summary_pass(summary_pass | default(''), source_scope | default('')) }}

{% if chapter_number | default(0) %}- 章节：第 {{ chapter_number }} 章{% endif %}
{% if chapter_title | default('') %}- 标题：{{ chapter_title }}{% endif %}
{% if outline | default({}) and outline.goal | default('') %}- 章节目标：{{ outline.goal }}{% endif %}
- 目标长度：约 {{ target_words | default(200) }} 字

## 输入文本
{{ chapter_text | default(text | default('')) }}

## 摘要内容要求

### summary（必填）
{{ target_words | default(200) }}字以内的高密度叙事摘要，按时间顺序覆盖本章核心情节。

### key_events（推荐）
按发生顺序列出3-8个关键事件，每个事件用一段纯文本字符串描述，不要写成对象结构。禁止使用嵌套字段。

### character_state_changes（推荐）
角色在本章中发生的状态变化（情绪/位置/关系/认知），列出角色名+变化描述。

### unresolved_questions（推荐）
本章结尾留下的未解悬念或开放问题。

### new_facts（推荐）
本章新确立的硬事实（人物/地点/规则/伏笔），供后续章节引用。

## 压缩规则
1. 只保留主事件、关键转折、角色状态变化、未决问题。
2. 删除描写性细节、重复对话、修辞性句子。
3. 时间顺序保持可追溯，便于后续章节承接。
4. 不要输出任何解释、标题或代码块。

## 质量校准
- 工作身份：你是一位章节记忆编辑，擅长把正文压缩为后续创作可直接承接的高密度叙事摘要。
- 摘要路径：先按时间顺序抽取核心事件，再补入角色状态、未解问题和新事实，最后压缩成可检索表述。
- 证据校准：摘要只承接本章实际发生或明确确立的信息；修辞气氛降为背景，不升级为硬事实。
- 可操作路径：每条摘要信息都应能被后续章节 Bridge、Memory 或 VolumeAudit 直接引用。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否保留后续合并/检索需要的事实边界，且没有把推测写成既定事实。

{#- ---------------------------------------------------------------------------
    格式层：输出格式与 Schema
    --------------------------------------------------------------------------- -#}

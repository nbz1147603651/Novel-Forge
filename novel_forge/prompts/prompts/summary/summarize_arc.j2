{%- from "_base/_summary_pass.j2" import render_summary_pass -%}
{#
================================================================================
【备注】
================================================================================
提示词：summarize_arc.j2
服务步骤：SUMMARIZE_ARC（故事弧摘要）
作用简述：将故事弧内容压缩为长期上下文注入用的高密度摘要
上游输入：
  - arc_name: 故事弧名称
  - target_words: 目标字数（默认 2000）
  - text/chapter_text: 输入文本
下游输出：
  - JSON：summary
  - 下游消费者：Memory 系统 / 长期上下文注入
消费链优先级：
  P0 = 保留弧线级变化（起点/转折/高潮/收束/遗留线索）
  P1 = 角色与主题只保留对主弧有影响的变化
  P2 = 不输出章节级细节列表
简要例子：
  输入：arc_name="林远的身世之谜", target_words=2000, text="..."
  输出：{"summary":"..."}
================================================================================
#}

{#- ---------------------------------------------------------------------------
    指导层：任务定义与输入消费
    --------------------------------------------------------------------------- -#}

请将以下内容压缩为故事弧级摘要，供长期上下文注入使用。

{{ render_summary_pass(summary_pass | default(''), source_scope | default('')) }}

{% if arc_name %}- 故事弧：{{ arc_name }}{% endif %}
- 目标长度：约 {{ target_words | default(2000) }} 字

## 输入文本
{{ text if text else chapter_text }}

## 输出字段说明
- `summary`：故事弧级高密度摘要，保留弧线级变化（起点/转折/高潮/收束/遗留线索），约 {{ target_words | default(2000) }} 字。

## 规则
1. 只保留弧线级变化：起点、关键转折、高潮、收束、遗留线索。
2. 角色与主题只保留对主弧推进有影响的变化。
3. 不输出章节级细节列表，不输出创作建议。

## 质量校准
- 工作身份：你是一位章节记忆编辑，擅长把正文压缩为后续创作可直接承接的高密度叙事摘要。
- 摘要路径：先按时间顺序抽取核心事件，再补入角色状态、未解问题和新事实，最后压缩成可检索表述。
- 证据校准：摘要只承接本章实际发生或明确确立的信息；修辞气氛降为背景，不升级为硬事实。
- 可操作路径：每条摘要信息都应能被后续章节 Bridge、Memory 或 VolumeAudit 直接引用。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否保留后续合并/检索需要的事实边界，且没有把推测写成既定事实。

{#- ---------------------------------------------------------------------------
    格式层：输出格式与 Schema
    --------------------------------------------------------------------------- -#}

"""Workflow pages.

Auto-discovered by ``pages/__init__.py`` via pkgutil.iter_modules.

Public entry point: :class:`WorkflowPage` in :mod:`.page`.
The actual page class is imported lazily by ``page_registrations.py``
to avoid triggering the deep workflow import chain on package init.
"""

from __future__ import annotations
"""Cross-platform code organized by subsystem.

Each subpackage wraps one OS-specific decision (sleep inhibit, ollama
binary lookup, font fallback table). Keeps desktop/ top-level clean
of platform branching.
"""

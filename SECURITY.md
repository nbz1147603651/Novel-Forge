# 安全与隐私

## 报告问题

请勿在公开 Issue、PR 或截图中发布可用密钥、令牌、私人作品及可直接利用的漏洞细节。

若仓库已启用 GitHub 私密漏洞报告，请使用仓库 **Security → Report a vulnerability**。若该入口不可用，可开一个不含敏感细节的 Issue，请维护者提供私密沟通方式，再发送复现材料。目前没有承诺的响应时限或长期支持版本；修复以当前维护分支为主。

## 数据与调用边界

程序默认面向本机使用。保留 `NOVEL_FORGE_API_LOCAL_ONLY=true`；确需远程服务时，应配置访问令牌、可信主机和传输保护，不能直接暴露开发服务器。

`.env`、`model_profiles.json`、`data/`、模型权重和运行日志属于本机内容。使用自定义存储目录时优先放在仓库外。日志、提示词、报错详情和截图可能包含小说正文、人物设定、音频路径及服务商信息，应先脱敏再分享。

本地存储不等于离线推理：选择云端模型、TTS、研究或媒体服务后，任务需要的内容会发送到相应服务。请按自己的数据要求选择 Provider。参考音频、声音克隆和生成媒体还须满足素材授权与服务条款。

## 提交前检查

```bash
python scripts/check_public_release.py
python scripts/check_public_release.py --history
git status --short
git diff --cached --stat
```

检查器仅报告路径、规则和对象标识，不打印密钥；它检查高置信度凭据模式及私密路径，并非所有隐私或秘密的完整检测器。二进制图像内容仍需人工检查。

`.gitignore` 只控制未跟踪文件。若密钥曾被提交，先在服务商端撤销／轮换，再协调清理所有相关分支、标签及远端历史；删除当前文件或撤回一个提交都不足以清除历史。历史重写会影响其他克隆，必须另行安排，不能在发布时忽略扫描失败。

# 开源发布准备

核对日期：2026-09-07。本项目以不含旧 Git 历史的源码快照建立公开基线；旧仓库保持私有，只作历史档案。

## 开源范围

建议公开可运行、可维护的程序：Python Engine、React/Tauri NIMO、仍有兼容依赖的 PySide6 后备端、提示词、必要测试、构建配置、使用说明及第三方声明。

主 UI 已从 PySide6 转向 React/Tauri，业务逻辑由统一 FastAPI Engine 提供。保留后备端不等于继续开发两套 UI；当前直接删除它会扩大为兼容性重构，不属于开源清理。

开源不要求公开全部开发资料或原始 Git 历史。用户作品、密钥、模型权重、日志、私人参考材料与本机工具缓存留在本地。历史审计保留工程证据，但不作为首页展示重点。由于旧历史存在敏感配置，优先考虑从清理后的源码快照建立公开仓库；若保留历史，则需另行清理所有相关引用。

## 已整理

- README 精简为程序介绍、MIT 许可、源码安装及文档入口；简述旧 UI 的迁移。
- 补齐许可证、贡献指南、安全说明、第三方来源及上游版权原文。
- 增加 GitHub Issue / PR 模板和项目链接；模板与 README 仍需推送后才会在远端生效。
- 去除重复 ignore 项，阻止示例目录中的运行锁、私有 JSON、凭据与缓存进入提交。
- 根目录四份历史审计归档；开发脚本及历史文档去除个人绝对路径；VS Code 不再引用私人工作树。
- `check_public_release.py` 与 CI 检查公开文件；`--history` 另查本地全部可达历史，输出不含密钥值。

## 核验与尚未完成事项

- 初始工作树仅有未跟踪的示例锁目录，未删除其中内容；现在由忽略规则保护。
- 当前公开内容检查通过；与本机配置中 3 个实际凭据值精确比对，没有匹配的公开文件。扫描不保证发现所有秘密或截图中的个人内容。
- 公开文档的本地链接、JSON/TOML 语法、新检查器的 ruff 检查、忽略与凭据识别探针通过。隔离 Git 仓库回归验证了“当前已删除但历史仍有凭据”的检测和输出脱敏。修改过的维护脚本仅做语法检查，没有运行其翻译、模型调用或数据库写入操作；AGENTS facts 检查通过。
- 清理前的历史扫描覆盖 28,627 个可达 blob，报告 7 个对象：旧模型配置、工具元数据和锁文件。其中旧模型配置含两条疑似真实 API 密钥。它们与当前 `.env` 密钥不同，是否已撤销未知。不能原样公开该历史。
- GitHub 发布采用“私有历史归档 + 同名干净公开仓库”的结构。公开仓库只从本次快照起保留历史，不继承私有归档中的分支、对象或疑似凭据。
- 项目维护者已确认仓库中的 NIMO Logo 与桌宠可以公开分发。`Reference/ai-flavor-remover-main` 继续排除在源码交付之外；若将来引入其中的实质内容，须在合入前补充来源和许可。
- 真实模型调用、打包程序分发和完整跨平台回归不在本次文档清理验证范围。打包发布须按实际依赖生成许可证清单，尤其注意 PySide6/Qt、FFmpeg 和模型权重条款。

## GitHub 展示

简介：**作者主导的 AI 小说创作工作台，基于 React/Tauri 与 Python/FastAPI，支持多模型协作。**

Topics：`creative-writing`、`novel-writing`、`llm`、`python`、`fastapi`、`react`、`tauri`。

主页链接保持为空，使用 README 作为介绍页即可，无需额外建设 GitHub Pages。公开仓库以当前源码树建立单提交基线，后续开发、Issue、PR、CI 与 Release 统一在公开仓库维护；私有归档不再同步新功能。

本批依赖清理前源码 `4d7af099`。不改变模型策略或作品格式；回退时仅逆向撤销本批源码／文档提交，不删除作品、配置、候选或费用证据。GitHub 简介和标签是独立元数据，Git 回退不会修改它们。

## 首页展示复核（2026-09-07）

首页补充虚构创意、四步创作流程与两张 NIMO 测试界面图，主界面迁移与 MIT 说明保持简短。截图直接引用现有自动化测试快照，不复制图片、不接触真实作品：

- `clients/nimo-desktop/tests/primary-surfaces.visual.spec.ts-snapshots/chapter-studio-prepared-narrative-ember-darwin.png`
- `clients/nimo-desktop/tests/primary-surfaces.visual.spec.ts-snapshots/voice-configured-narrative-ember-darwin.png`

逐图检查未见私人路径、密钥或真实账号信息，两图均无 EXIF 和 PNG 文本元数据；页面明确标注合成项目和测试版本，不冒充最新共创验收。Logo SVG 不含脚本、外链或个人信息；维护者已确认 Logo 与桌宠可公开分发。移除依赖外部加载的徽章，保留普通 CI 链接。

本地 Markdown 浏览器预览检查了截图显示与展开操作，源文件链接及公开内容检查通过。此次历史扫描覆盖 28,668 个可达 blob，仍有 6 个对象命中（工具元数据和含疑似密钥的旧模型配置）。对象计数会受 Git 对重复内容的路径选择影响，计数变化不表示已清理历史。本批只修改介绍与复核记录，不推送、不改仓库可见性。


## 当前架构截图替换与画布修复（2026-09-07）

本批取代上节两张历史测试截图的首页引用；保留原测试基线供回归使用。README 现展示九张当前 React 应用的合成数据截图，重点补齐人物图谱、叙事蓝图与映界画布，来源和验证边界见 [截图记录](images/nimo/README.md)。用户提供的《药证》截图不纳入公开文件。画布并未移除，原入口仅在策划页；本批补充阶段栏旁的专家画布入口。

当前公开内容检查和图片元数据检查通过。旧历史中的疑似凭据不进入公开快照；旧仓库继续保持私有。Logo 与桌宠公开分发已由维护者确认。

## 首个公开基线（2026-09-07）

公开基线取自私有开发提交 `e2ffde21` 之后的已审核源码树，并加入本节授权与仓库管理结论。快照的文件树与来源提交一致，另建单提交 `main` 历史；当前文件和快照全部可达历史的发布检查均为 0 个发现。

维护结构固定如下：`Novel-Forge` 是唯一公开开发主线；原仓库更名为 `Novel-Forge-private-archive` 并保持私有，只保留旧历史。公开仓库不得合并私有归档分支或强制推送旧提交，以免重新引入私有对象。旧历史中出现过的凭据仍应撤销或轮换，归档私有不能代替凭据失效。

首次公开 CI 的源码卫生作业通过，并发现两个此前未在本机暴露的基线问题：初始化上下文缺少一个既有影子执行标志的显式类型声明；PySide 后备端在 Ubuntu 缺少 Qt Multimedia 所需的 PulseAudio 运行库，Windows 又无法解释 POSIX 反斜杠续行。修正只补齐类型契约和 CI 环境，不改变创作、授权或媒体逻辑。

第二轮公开 CI 进一步确认三项基线差异：lint 环境没有安装完整的可选类型依赖，Windows 无 `fcntl`，首次会话恢复可能覆盖启动后已发生的 PySide 导航。此前被忽略的 `uv.lock` 现纳入公开版本控制，lint 从该锁文件安装全部 extras，避免 SDK 或 PySide 版本随 runner 漂移。由于 PySide 的平台 wheel 暴露不同类型表面，历史 mypy 债务清单固定在其生成平台 macOS；Linux/Windows 继续运行实际单元与后备端兼容测试。拟人化词库以 POSIX `flock` / Windows 字节区间锁维持同一排他锁合同；会话恢复尊重较新的用户导航。公开源码卫生继续独立运行，不因兼容修正降低检查。完整三平台 CI 通过后，以对应公开提交作为首个可维护基线。

锁定环境还让完整导入契约稳定检出一条既有 persistence → StoryKernel schema 分层违例。章节清理现通过最小结构协议消费已由允许的 store 边界加载并校验的对象，不新增例外规则，也不改变 Canon 回滚内容。

依赖安全作业保留 `pip-audit --strict`，并显式跳过尚未发布到 PyPI 的 editable 本地包；其全部已安装第三方依赖仍严格审计。首次失败仅为审计器无法在 PyPI 解析 `novel-forge 0.1.0`，日志没有报告第三方漏洞。

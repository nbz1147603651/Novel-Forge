# 共创发布验收记录

本记录只写已执行的验证，不把固定离线响应当作真实模型质量对照。

## 当前状态 / B7n 最终检查（2026-08-28）

依赖 B7m `5a1954ba`，本地发布提交为 `7f2a1157`。正式能力 `authoring_coauthor` 已开放；不会自动创建旧项目策略、授予权限或启动任务。关闭能力仍保留读取/停止并拒绝新执行的测试显式模拟关闭状态。临时浏览器后端删除能力覆盖，跟随正式开关，聊天仍是固定离线响应。

本批开放状态下已执行：

| 检查 | 结果 |
| --- | --- |
| 28 文件相关 Python 回归（含真实 MockAdapter 共创整章） | 340 passed；2 条 Starlette/httpx 依赖弃用警告 |
| Qt 共创窗口串行 offscreen | 5 passed |
| 全契约消费者：`check_contract_consumers.py --base 0f47e15f` | 106 项 Python API/job、623 项 Vitest、应用 TypeScript 全通过 |
| 浏览器夹具 TypeScript | 通过 |
| Python 全目录 ruff | 通过 |
| strict mypy 债务基线 | 通过，既有 1803 项基线未扩大，未改基线 |
| 两条 import-linter 架构约束 | 通过，无豁免 |
| 提示词层、全格式审计、提示词包、生成索引、模板和格式契约 | 全通过 |
| 控制面约束、设计 token、调用点同步 | 全通过 |

上述集合存在重叠，不相加宣称全仓测试总数。未运行全仓全部测试、真实付费模型质量/费用对照或打包应用端到端验收。实际浏览器场景见 B7j，100 章仅为规划水位及恢复模拟；原文生成与终稿验收由 MockAdapter 集成覆盖。旧无版本化的全书维护/记忆重置在配置共创项目中明确拒绝，不宣称已具备这些操作的提案版本。现有后端必须安全停止并重启才采用新源码。

初轮 B0a–B7j 的 18 次回退与 B7k–B7n 的 4 次追加回退均已完成，分两段相接的独立 worktree 演练。没有在当前开发分支执行回退，没有清理作品或费用证据。

以下各节是当批历史记录，较早的“开关关闭/等待检查”由本节最新状态覆盖。

## B7o 追加回退与交付记录（2026-08-28）

此批仅更新文档，依赖发布提交 `7f2a1157`。新建 detached worktree `/tmp/nimo-authoring-release-rollback.RmpYtM`，复制已停用的临时测试书，不连接用户作品。初轮夹具没有真实费用，仅任务历史中的费用字段为零；本次另用正式费用存储接口建立两条明确标注 `fixture_synthetic` 的合成记录：已结算 `0.0123`、未确认占用 `0.04`（USD 单位字段，均非真实收费）。没有调用模型或充值/退款。

| 批次 | 原提交 | 演练中的逆向提交 |
| --- | --- | --- |
| B7n | `7f2a1157` | `04d0696c` |
| B7m | `5a1954ba` | `de79c8ed` |
| B7l | `69275c64` | `9580c64a` |
| B7k | `d12eb760` | `146bca52` |

四次 `git revert --no-edit` 均无冲突，未关闭钩子；`git diff --stat 497278fb HEAD` 为空。演练前基线已包含合成账本和停用服务重载；四次代码回退前后全部 24 份文件 SHA-256 相同。回退到 B7j 后启动 JobService，只恢复一条已完成聊天历史，活动/排队/停止中 worker 均为零；作品仍停用。既有安全启动逻辑把停止策略版本从 9 递增至 10，旧批准不恢复；除这份策略记录外，其余 23 份正文、人工修订、后续章、提案、任务和费用证据全部不变。回退代码下 5 项能力停用/恢复回归通过。

再复制这 24 份文件到原始基线演练目录的 `data-rollback-budget-fixture`，以源码树等于 `0f47e15f` 的旧 JobService 启动：一条已完成历史、零 worker，所有 24 份文件哈希仍不变；只读核验费用库保留两条合成记录。副本互相独立，没有将旧全项目快照回写当前作品。每个中间逆向版本未重复跑全仓测试，不能据此声称所有历史组合均完成全量验收。

本地代码/数据恢复证据链至此完成。实际部署回退时先停用、等待合法原子提交和线程安全退出，再按依赖逆序撤销；B7o 是纯文档，精确回退代码与文档时先撤销 B7o 再撤销 B7n。不使用 reset，不删除修订/提案/费用，不自动恢复旧授权。当前开发分支保持新实现，未推送；真实小说和付费模型未参与演练。

## B7j 浏览器与原生端（2026-08-28）

使用真实 `ProjectsReader`、`AuthoringWorkspace`、HTTP 客户端、作者 API、JobService、提案审批和正文修订入口。后端强制创建临时目录，不接受用户作品路径；聊天用固定离线响应，未构造模型网关。其余设定文档为原有 UI 夹具，不是对该作品内容的质量验证。

手工浏览器交互已验证：

- 默认收起；新项目推荐本章共创，保存与启用均无任务；旧授权未自动升级。
- 从设定切到章节，共创上下文随之切换；普通讨论只保存候选，未覆盖正文。
- 批准框显示原文、候选、风险和费用；未勾选明确确认不能提交。批准后正文成为新修订，后续章保留，报告/发布状态标为待重验。
- 已保存授权可暂停、停用；能力协商关闭仍保留停止入口，不能批准、启动或重新启用。重新启用作品后仍保持停止，任务数未增长。
- 关闭侧栏和关闭能力协商均不丢当前未保存人工改稿。
- 第二个浏览器窗口人工编辑后，旧候选批准返回过期；原因跨后台刷新保留。拒绝过期候选不再生成替代方案。
- 1280px 阅读布局保留原导航。用户指出窄窗标签层穿透后，隔离阅读区层级、去掉阅读器在共创网格内的负外边距；≤1050px 改用已有弹层基础设施承载不透明抽屉。749px 窗口实测抽屉完整落在视口内，命中测试不再点到后面的标签栏；审批框盖在抽屉上，Escape 逐层关闭并恢复焦点，未保存人工草稿仍完整。预览容器按窗口宽度缩放，正式桌面的全局最小宽度未被改写。
- 后端源码变化时，现有 JobService 拒绝新模型任务并要求安全重启；已归档章未重写。

共创人工改稿保存预览已复验：明确“保存为待批准提案”，不再让用户选择后端不会采用的直接保存范围或后台重评。浏览器没有执行真实章节生成或付费模型；章节生成、归档门禁、渐进规划由独立离线管线回归覆盖。原生 Qt offscreen 交互覆盖逐版本批准、过期竞争、暂停/启用及本章默认范围。完整能力仍关闭，后续还需终稿二次验收和代码回退演练。

复现入口（只绑定本机）：

1. 在仓库根运行 `.venv/bin/python tools/ui-parity/authoring_fixture_server.py`。
2. 在 `clients/nimo-desktop` 运行 `pnpm run dev`。
3. 浏览器打开 `http://127.0.0.1:1420/tests/browser/authoring.html`。
4. 源码变化后安全停止并重启夹具；它会新建空测试作品，不重用用户作品。

测试入口不被正式应用导入或打包；真实费用对照仍须单独授权样本和预算。

## B7k 终稿与逐批回退（2026-08-28）

真实 `persist_results` 与检查点序列化回归：终检后的新正文必须重新验收；旧批准不写正文/正史，新批准恢复时不重复可选去味，事实门禁再次执行。等待和重启不覆盖既有正文。27 文件离线回归 318 passed / 5 skipped，跳过项均为禁止 xdist 的 Qt 用例，随后串行 5 passed。全消费者 106 项 Python API/job、623 项 Vitest、TS 通过。

发现一个旧集成夹具错误：构造无水位的旧检查点，却断言另一路过期提示。相同失败已在原基线 `0f47e15f` 单独复现；现分为“水位缺失”和“水位从 2 变为 1”两例，断言结构化原因和正文未变化，生产门禁未改。

回退范围为截至 B7j 的全部 18 批。先用真实界面/API 暂停、停用临时测试书，再复制该测试书至独立 worktree `/tmp/nimo-authoring-rollback.HJTZUj/data-rollback-fixture`。从 `497278fb` detached checkout 开始逐一 `git revert --no-edit`，不关闭钩子、不使用 reset、不碰当前分支/用户作品。每次无冲突完成后执行下一项；未在每个中间版本重跑全量测试：

| 批次 | 原提交 | 演练中的逆向提交 |
| --- | --- | --- |
| B7j | `497278fb` | `bcb434f7` |
| B7i | `d77900c9` | `f20ebb62` |
| B7h | `f7c17db2` | `a14e11d7` |
| B7g | `719b04c4` | `6ab306a8` |
| B7f | `aa06e2ab` | `eee0306b` |
| B7e | `5d72f330` | `b0c24ccf` |
| B7d | `338f1a49` | `3b665ec2` |
| B7c | `47593eed` | `98a26d4c` |
| B7b | `d6740143` | `8bdc2b98` |
| B7a | `a454794c` | `82d59d24` |
| B6 | `122a586a` | `c11ab519` |
| B5 | `fc655e68` | `19facac3` |
| B4 | `84045ddb` | `125c1833` |
| B3 | `efea86c4` | `247997df` |
| B2 | `2e72b32a` | `519e39d4` |
| B1 | `938133b0` | `e9ba2580` |
| B0b | `4c5e42b5` | `17d9f97c` |
| B0a | `df7987be` | `43357615` |

`git diff --stat 0f47e15f HEAD` 为空，即跟踪源码完全回到基线。测试书 23 个文件在演练前后 SHA-256 一致，包含后续章节、人工修改、候选/批准、修订证据、停用标记和带零费用字段的任务记录（初轮没有独立费用账本；B7o 追加合成账本演练）。随后以旧代码启动 JobService，仅恢复一条已完成聊天历史，活动 worker 为零；这 23 个文件再次核对不变。重新启用仍需用户主动处理。

演练提交只存在于临时 detached worktree；这里的逆向提交用于审计，不是让当前分支 cherry-pick。实际回退先停用/等安全停止，再从当前已部署版本按依赖逆序 revert。B7k 本身只有测试/文档，完整能力开放仍须最后发布检查。

## B7l 记忆入口核验（2026-08-28）

压缩/批评 API 的章段、停用前拦截、调用中暂停、模拟费用持久化和未提供章号拒绝均有回归；HTTP 全书记忆重置/重建返回 409，workspace 母题/向量重建在记忆加载前被拒绝。未配置项目的原有压缩、批评、向量重建测试仍通过。正常归档记忆管线没有改动。84 项针对性 Python、106 项 API/job、623 项 Vitest、TS、mypy 与两条导入边界通过；费用测试使用合成数值，无真实模型消费。该子批尚未纳入上面的 18 批实际逆序演练，最终发布时补入。

## B7m 跨章与整章共创（2026-08-28）

新增单章入口负向用例先在旧实现复现“前章未归档仍可开始下一章”。修复后检查前章已归档、正史、过期关系和同版必需提交；提案执行前和最终持久化重新核验。共创 API 投影给出相同等待原因。单独讨论、未来补纲候选仍开放。

`test_coauthor_chapter_requires_plan_and_exact_final_acceptance` 使用真实初始化/章节管线和 MockAdapter：方案未批不能生成，方案批准后生成审校；正文未批不归档，终检改变正文触发新检查点，旧批准失效；再次验收后同版必需状态完整，才允许准备下一章，下一章正文仍未生成。这不是付费模型质量测试。

340 项相关 Python、5 项串行 Qt、106 项 API/job、623 项 Vitest、TypeScript、mypy 基线和两条架构约束通过。六项提案测试的上游夹具补齐实际终结清单，不豁免生产门禁。完整开关仍待最终发布检查，B7k–B7m 的追加回退尚待记录。

## R2 统一修复工作台（2026-08-31）

使用真实 `RepairWorkbench`、HTTP 客户端和 authoring repair API，后端夹具只指向一次性项目，没有模型网关、正式 repair plugin 或 publisher。浏览器实测：

- 1280×720 和 749×900 窗口下全屏层均不溢出，窄窗字段和问题列表可读，无页面穿透。
- 圈选官方正文建立人工问题，定位显示精确 `0–21`、`exact_utf8_span_v1`、稳定节点、容器哈希、原始/规范化值；官方源文权限为 `proposal_required`。
- 人工编辑只保存隔离候选，状态回到 `needs_verification`，事件轨迹保留 `case_created / targets_resolved / candidate_edited`。未迁移的章节插件不能复验，发布按钮始终关闭。
- 底层未保存草稿设为“下层草稿（未保存）”后打开工作台，Escape 只关一层，草稿未丢失，焦点返回“打开修复工作台”按钮。
- 验收后查询夹具：官方正文和第 2 章均未变，`jobs=0`、`modelCalls=0`；新鲜页面 console 无 error/warn。

Python 定向 29 项、全量 NIMO Vitest 102 文件/653 项及 TypeScript 检查通过。这是 R2 读/人工编辑骨架验收，不是短篇、TTS、初始化、长篇或全书修复已切换。回退前安全停止工作，不删除 repair 案例、作品、费用或回执。

## R3 短篇与 TTS 候选试点（2026-09-01）

- 短篇离线夹具证明：旧字符串诊断会产生绑定源哈希、稳定节点和精确字符范围的结构化 issue；完整度/质量修复只写隔离候选。非空但完整评价更差的候选被拒绝，严格更优且完整度、作者意图通过的候选才采用；候选专用评价不会覆盖正式报告。
- TTS 夹具证明：定位使用规则审查后基线内容生成的 `segment_uid + field_path`，候选 UID 只作结果身份；小说源文字段不可修改。源哈希、段集合、源文本、完整度和未解说话人任一门禁失败均在返回/持久化脚本前抛错，验证证据随 `DubbingScript` 元数据保留且不参与内容哈希。
- `repair_case` 已加入现有 JobService，耐久意图可恢复，但请求只接受项目、案例、操作和 CAS 版本；只执行 prepare、verify、shadow_compare。影子比较不改变案例状态，不包含 publish 操作。
- 正式短篇发布仍由原阶段边界控制；TTS 只能发布派生 `DubbingScript`，不能写小说源文本。没有注册通用正式 publisher，没有执行真实 provider 影子抽样，也没有增加 PySide 工作台。

148 项核心/任务定向 Python、494 项 TTS、18 项架构/审查测试、TypeScript、ruff、mypy 债务基线、提示词与格式合同检查通过。全量 NIMO Vitest 和最终事实漂移检查在本批提交门禁中另行执行。回退前安全停止相关任务，保留 repair 事件、候选、费用和已有回执；不得用恢复重放旧候选覆盖后来内容。

## R4 初始化候选优先迁移（2026-09-01）

- 通用初始化证据层将旧 `InitRepairIssue`/结构化 issue 投影为 `AuditIssueV2`，保留精确 JSON Pointer、稳定节点、容器哈希、源版本、`expected_raw/actual_raw`、双方规范值和 comparator。Pydantic `loc` 不再只变成人类字段文案。
- 回放失败截图同类的 `[3, 5] 日` 与 `[3,5]日`：原始值保留不同，两个 normalized 值均为 `[3,5]日`，比较器记为 `init_duration_range_v1`。因此工作台合同能解释“看起来一样”的真实输入，规范等值不再被当作冲突证据。
- `InitRepairOrchestrator` 在不改领域阶段顺序的前提下绑定候选哈希、精确 patch 和原 policy 校验。LLM 失败候选不再传给本地 fallback；两者均从同一未修改基线起步。若全部复验失败，输出回到原 payload 和原报告。
- 契约一致性确定性修复改为深拷贝候选；原调用 payload 在复验前不变。同一 handler 作为后置校验器，仍可修、缺失或抛错都拒绝采用。通过后候选、验证束和结构化问题同时记入 `init_artifact_repair.json`。
- 重复 JSON Pointer 导航实现收口到共享 resolver，嵌套对象、列表、`~0/~1` 转义和空键兼容回归通过。已配置项目仍在初始化直写前由 foundation/PlanningRevision 守卫拦截，本批没有新增 NIMO 导航、主题、模态、PySide 产品面或发布权限。

69 项核心候选/定位回归、772 项初始化/基础资料守卫/集成/快照测试、30 项候选/API-job/架构核心、653 项 NIMO Vitest 及 TypeScript、全量 Python ruff、mypy 债务基线（1803）、提示词层/索引/模板/格式合同和 AGENTS 事实检查通过。额外 API/job 组合中 234 项通过、1 项跳过；唯一失败是 R4 之前已存在的 `planning` 视图过期精确字典断言，当前接口多返回既有 `archived_chapters/task` 字段；R4 与该实现/测试相对 `f192bac4` 均无差异，本批不删除 UI 字段来规避旧断言。没有 provider 调用、项目作品写入或实际 UI 改动。本批尚未把自动初始化候选建成项目级 RepairCase，因此旧初始化报告是当前持久证据入口；长篇/全书切换仍属 R5/R6。回退本批前安全停止初始化任务，保留初始化报告、候选、费用和已有基础资料提案/回执。

## R5 长篇工作候选迁移（2026-09-01）

- 连贯性、因果和追读力共用循环现在在进入修复前固化必修 issue、源文哈希和原维度验证器；复验证据绑定最终候选哈希。外层只在原审查实际运行、原问题关闭、无残留、无新高/严重回归、未耗尽且不需人工时保留改文。
- 任一门禁失败，外层 snapshot 恢复该维度的输入文本及报告；后一维度不会继承被拒候选。多轮内层只从上一个已复验文本继续，回归轮会回到它的轮前版本。追读力不再在复验前写入 `v96_edited.md`。
- 每个可唯一重绑的 issue 投影为 `.authoring/repair_cases` 中的 `automatic_working_candidate`，保存源/候选 blob、精确 issue 字符窗口、全章工作补丁身份、原验证器结果及作者意图/锁定/结局/命运/世界规则/归档正文保护项。重复引文或无段落锚点的 issue 降为 `manual_required`，不建候选。
- 工作台不新建页面：原问题/定位/候选/验证/历史层补三类长篇筛选、中文来源/权限标签。后端字符区间与前端高亮统一为 Unicode code point，emoji 在目标之前也不偏移。阅读器不卸载，既有窄窗、Escape 单层关闭、焦点恢复和脏草稿语义不变；PySide 无新 UI。

675 项长篇/修复 Python 回归、118 项 API/job/架构核心、654 项 NIMO Vitest、TypeScript、全量 Python ruff、mypy 债务基线（1803）、提示词层/格式/索引/模板合同和 AGENTS 事实检查通过。额外的全量并行单元尝试仍触发仓库既有的 Qt/xdist fixture 与 provider circuit 噪声，不能记为全量通过；其中可稳定复现的两项 PySide 设置页架构用例因测试 fixture 缺少 `_ollama_status_badge` 失败，R5 未改 PySide、设置页或任务绑定。本批没有真实 provider 调用、作品迁移、正式章节 publisher 或 PySide 产品面。正文仍只能经终稿逐版接受和归档屏障推进；全书原子批次与真实影子调用留给 R6/R7。回退本批前安全停止长篇任务，保留报告、RepairCase/候选 blob、费用、后来正文与回执证据，不用候选恢复覆盖后来修改。

# Fidelity Ledger

Record only deliberate, reviewed deviations from the PySide6 reference.
An empty table means no approved deviations exist.

| Surface | Reference evidence | Difference | Reason | Owner | Decision |
| --- | --- | --- | --- | --- | --- |
| Shared shell and compact primary-page chrome | Fresh 1440×900 PySide6/Tauri dashboard and workflow captures, `narrative_ember`, 2026-07-18 | React uses the 32px/12px shared control tier and reduced rail/top-bar/primary-page geometry instead of the old client’s larger title, hero and button geometry. | Explicit user direction: keep more of the active creation surface visible and avoid ornamental whitespace. | UI parity | Approved product-density exception. Do not use full-frame strict pixel threshold for compact-primary screens; retain source-sized component threshold and semantic/interaction evidence. |
| Dashboard search field placement | 2026-07-22 PySide6 offscreen golden vs React Chromium pair (`comparison-pairs/dashboard--narrative_ember--loaded.png`) | React relocates the 卷库 search input from the 卷库总览 section into the hero card; PySide6 keeps it below the library summary. | Compact-primary density direction: co-locate the most-used filter with the welcome card to reduce vertical scroll. | UI parity | Approved layout deviation. Covered by `project-operations.spec.ts` (“案头将卷库检索收进欢迎卡的空白区”). Not a pixel-parity target. |
| 2026-07-22 full-frame screenshot comparison | 33 PySide6 offscreen goldens vs 33 React Chromium captures, 5 themes + key states (`screenshot-comparison-report.md`, `comparison-pairs/comparison-summary.csv`) | Full-frame changed_pixel_ratio 0.50–0.87 (0/33 at 0.75% threshold); pair review shows identical section/component/state structure with only density + font-rasterization offset. | Differences decompose into the approved density exception, the approved search relocation, and inherent Qt↔Chromium glyph rasterization — none reversible without undoing approved product direction. | UI parity | Structural/semantic parity accepted for compact-primary screens; full-frame pixel threshold waived per the density exception above. Interaction parity separately proven by 47 Playwright behavior specs + `check-interaction-consistency.js` (5/5). |
| Dashboard hero fixture divergence (non-production) | 2026-07-22 pair: PySide6 hero shows empty-state “案头未陈一卷” while React shows featured “案头当看：测试短篇” despite both having 2 projects. | PySide6 visual fixture `build_visual_workspace_snapshot` sets `featured_project=None`; React mock derives featured = `projects[0]`. | React hero logic matches production semantics (a populated workspace shows a featured project); the empty-hero-with-projects state is a fixture artifact, not a component defect. | UI parity | Tracked comparison-fixture gap only; no production change. Optional future alignment: let the React parity fixture express “projects present but no featured” so the empty-hero state can be captured for pairing. |

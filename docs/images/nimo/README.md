# 当前 NIMO 界面展示

拍摄日期：2026-09-07。来源为本仓库 `53fb375c` 后的当前工作树，包含同批映界画布入口修复。

使用 `VITE_ENGINE_MODE=mock pnpm --filter @nimo/desktop exec vite --host 127.0.0.1 --port 1438 --strictPort` 启动标准 React 应用，在浏览器中实际操作并截图。未启用旧 UI parity 专用页面。React 组件同时供 Tauri 使用；本次未验收原生打包客户端。

| 图片 | 实际页面 |
| --- | --- |
| dashboard.jpg | 案头作品与任务总览 |
| tasks.jpg | 专注任务详情 / 当前节点 |
| blueprint.jpg | 卷帙 / 叙事蓝图 / 全部轨道 |
| characters.jpg | 卷帙 / 基础设定 / 角色与实体 / 图谱 |
| workflow.jpg | 机杼 / 长篇初始化（模拟执行中） |
| chapter.jpg | 章台 / 章节轨道与上下文 |
| voice.jpg | 声腔 / 配音脚本 |
| film.jpg | 映界 / 长篇剧集 / 视觉资产 |
| film-canvas.jpg | 映界 / 专家画布 / 专注模式 |

图片是未经拼接或生成的 JPEG 截图，采用仓库 Mock Engine 的合成项目、人物与任务数据。界面中的 Provider、Token、进度、版本和费用相关状态都是模拟值，不表示真实调用或生成效果。部分能力按 Mock capability 禁用；截图不作为后端能力的完整验收证明。

未使用用户提供的《药证》截图，以免将私人作品、项目 ID、人物及配音描述公开。逐图检查未发现密钥、私人账号或个人目录；截图中的 `/tmp/…` 来自合成 fixture。JPEG 元数据检查无 EXIF、GPS 或注释字段。项目维护者已确认 NIMO Logo 与桌宠可以随项目公开分发。

浏览器核验：页面身份和正文渲染正常；蓝图图层切换、人物图谱缩放、任务详情展开、影视形态切换正常。画布验证覆盖视觉阶段直达、同一策划页从引导模式再次直达、适配全图、进入和退出专注模式、返回视觉阶段。未启动模型任务；错误/警告日志为空。

## 项目风格约束（编辑修复）｜项目专属风格规范
- 风格概述：Concrete suspense.

请根据以下护栏违规报告，对章节进行**定向修复**。

## Source Artifact 边界（P0）

- 缺少 `cards.source`：本步骤没有初始化权威投影，应由上游 gate 阻断；不要凭空补全 story_bible、character_bible 或 narrative_contract。

## ticket/window 规则
- 先看 guardrail_report 的 violation type、location、description 和修复建议。
- chapter_plan / alignment_report 只用于保护既有主线推进点和验收，不用于新增剧情或重排章节。
- 只修 ticket/window 覆盖的违规文本，不自由重写整章。

## 第 3 章
## 🔒 主线推进点保护（绝对不可修改）

以下主线推进点是本章的叙事核心，**修复过程中不得修改、删除或弱化**包含这些内容的段落：

- **主线点 1**：Lin Wan verifies the checksum anomaly.

### 保护规则
1. **定位**：在正文中找到实现上述主线推进点的段落
2. **冻结**：这些段落的内容不得被修改、重写或删除
3. **绕行**：修复操作必须绕过这些段落，仅修改非主线推进区域
4. **验证**：修复后必须确认所有主线推进点仍然完整存在

## 📊 大纲对齐关系保护

当前对齐分数（仅作参考，实际分数由下游 AlignmentStep 复检）：**8.0 / 10.0**

### 对齐保护规则
1. 修复后必须保持所有主线推进点完整存在，不得删除、弱化或改写其叙事结果。
2. 不得削弱已建立的大纲对齐关系；若 alignment_report 中有正面副线点，应保留其支持主线的功能。
3. 若当前对齐分已经偏低，这不是继续降低对齐质量的许可；修复只能围绕护栏违规做最小必要调整。
4. 不要在正文中输出对齐分、自评、复检说明或任何非正文标记。
5. 下游对齐分计算基于：
   - 主线推进点完整度（每个缺失扣 2.0 分）
   - 副线推进质量（弱副线过多扣 0.4 分）
   - 正面副线支持（每个加 0.2 分）

### 当前对齐报告摘要

- **正面副线点**：

  - checksum clue supports the main plot

- **摘要**：Main plot point is present.

## 🎯 护栏违规报告

### 违规详情

- **违规 1**：
  - 类型：future_leak
  - 严重程度：high
  - 位置：paragraph 1
  - 描述：final sender identity is revealed too early

  - 修复建议：hide the sender field

## 修复范围限制

### 允许修改的区域
1. 不包含主线推进点的段落
2. 过渡性描写、环境描写、心理描写
3. 副线推进相关的段落（在不影响主线的前提下）
4. 开头/结尾的衔接段落（如果不包含主线点）

### 禁止修改的区域
1. **包含主线推进点的段落**（绝对禁止）
2. 已经通过护栏检查的段落
3. 与当前违规无关的角色对话
4. 已建立的场景设定（除非违规直接相关）

### 修复策略
1. **最小干预原则**：仅修改与违规直接相关的部分
2. **局部重写**：优先局部修改，避免全文重写
3. **保持连贯**：修复后必须保持叙事连贯性
4. **字数合规**：修复后字数必须在 2000 - 3600 之间
5. **不制造新违规**：不得为修复某个护栏问题新增计划外 POV、正面出场角色、组织、关键道具或重大事件；不得改变 opening_contract / closing_contract / 已完成主线结果。
6. **禁出/禁揭示优先**：若违规涉及禁止角色出场、禁止主动联系或禁止提前揭示，只能删除、改写或转为可观察间接痕迹，不能换成同一主体的近义表述继续出现。
7. **字数防膨胀**：若需要补一句修复，必须同时压缩同区域低信息旁白，避免修复后章节结构性超长。## 待修复正文

Lin Wan saw the sender name and understood everything.

## 质量校准
- 工作身份：你是一位谨慎的小说修复编辑，擅长在保留已验证文本的前提下完成最小必要修复。
- 修复路径：先定位必须修的证据点，再确认允许改动范围，最后用最小文本变化恢复连贯、因果或表达质量。
- 字段级质量示例（非输出格式）：平弱="整体重写"；高质量="只改冲突句/缺口段，并保留已验证的人物、事件和语气"。
- 可操作路径：修复必须服务于原问题和下游验证，不扩写新剧情，不改变未被授权的章节事实。

## 交付前核验
- P0/P1/P2 优先级是否被保持，且没有让弱参考覆盖硬约束。
- 是否能把关键判断回指到输入证据、上游契约或已批准计划。
- 是否避免新增未授权的人物、地点、世界规则、未来 payoff 或格式协议。
- 是否保持修复范围最小，且修复后文本仍能通过原问题复核。## 修复成稿语义约束

1. **修复成稿必须是完整正文**，不包含说明、注释或结构化数据
2. **保持原有段落结构**，仅修改需要修复的部分
3. **主线推进点必须原样保留**，不得有任何改动
4. **修复后自检**：
   - 所有主线推进点是否完整存在？
   - 主线推进点和大纲对齐关系是否未被削弱？
   - 护栏违规是否已修复？
   - 是否没有新增计划外 POV、禁出角色、重大事件或后续揭秘？
   - 字数是否在约束范围内？
   - 是否没有密集使用定义式否定转折来自证描写品质？

- 具体化落到角色选择、动作后果、对白潜台词、环境阻力或场面调度。
- 不使用“不是……而是……”这类定义式否定转折来自证描写品质；除非角色正在辩驳、事实纠偏、时间纠偏或形成关键悬置。此规则禁止的是叙述者为描写质量做自我注解（如“不是冷，而是刺骨的寒”这类自我证明描写高级的写法），不禁止角色在剧情中做出真实区分、事实纠正或推动情节的否定转折。
- 抽象判断句只在辩驳、事实纠偏、时间纠偏或关键悬置中保留。
- 必达事件或旧意象需要保留时，只保留叙事功能，换触发点、动作链或观察角度。
- 同构句式反复承担同一功能时，只留真正改变读者认知的一处，其余改成反应、误判、成本、留白或外部变化。

## 统一格式契约（系统注入）
- contract_id: `generic:text`
- contract_mode: `text_only`
- 输出必须是纯小说正文或修订后的正文文本。
- 不要输出章名/题名；禁止 `# 第一章 标题`、`第 1 章《标题》` 等开头包装，直接从第一句叙事正文开始。
- 禁止 JSON、Markdown 代码块、标题、清单、修改说明、自检过程和元注释。
- 禁止泄露规划/系统字段，例如 `scene_intent`、`opening_contract`、`opening_bridge`、`required_outcome`、`exit_target_state`。
- 若内部自检发现上述内容，必须先重写，最终只输出可归档正文。

## 语言文字硬约束
- 输出语言：简体中文（zh-Hans）。中文内容必须统一使用简体字，禁止输出繁体字、异体字或繁简混排；专有名词和必要英文术语可保留原文。